# 后端 Host (.NET/ABP) 本地开发环境变量示例
# 复制为同目录下的 .env 后再根据实际填写。

# 数据库连接串（必填）
ConnectionStrings__Default=Server=localhost;Port=27017;Database=radish;Username=;Password=
# 如有第二套连接（按代码约定保留该键名）。
ConnectionStrings__Chrelyonly=

# ASP.NET Core 监听地址（可选，默认使用 launchSettings 或 Kestrel 绑定）
# ASPNETCORE_URLS=https://localhost:44342;http://localhost:44341

# CORS 允许的前端地址（可选，多个以逗号分隔）
# App__CorsOrigins=https://localhost:5173,https://localhost:4200

# 其他敏感/环境相关配置请继续放在 .env，避免提交到仓库。

