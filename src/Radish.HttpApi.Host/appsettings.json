{
  "App": {
    "SelfUrl": "https://localhost:44342",
    "AngularUrl": "http://localhost:4200",
    // 说明：CORS 允许来源已改为通过 .env 配置项 App__CorsOrigins 管理
    // 理由：
    //  1) 避免把个人本地端口/域名差异提交到仓库；
    //  2) 便于不同环境（dev/staging/prod）通过环境变量切换；
    //  3) 与前端统一：React/Angular 均走本地 HTTPS；
    // 如需参考旧值，可暂留在下行（已注释，不再生效）：
    // "CorsOrigins": "https://*.Radish.com,https://localhost:4200,http://localhost:4200,https://localhost:5173,http://localhost:5173",
    "RedirectAllowedUrls": "https://localhost:4200,http://localhost:4200,https://localhost:5173,http://localhost:5173",
    "DisablePII": false,
    "HealthCheckUrl": "/health-status"
  },
  // MongoDB 数据库链接，务必和 DbMigrator 项目配置文件一致
  // Default 是默认配置库，连接字符串名称不可更改！！！
  // Chrelyonly 是自定义库，可以更改连接字符串，在 [ConnectionStringName("Default")] 中配置即可
  // [ConnectionStringName("Chrelyonly")] 定义的库不可用时自动使用 Default 库
  // Default 库用来存储用户、Application 等基本服务，Chrelyonly 自定义库仅可以用来存储实体类
  // 配置类：RadishMongoDbTestModule 和 RadishMongoDbContext
  // 在 RadishMongoDbContext 中配置 [ConnectionStringName("Default")] 要使用的连接字符串
  // 在 RadishMongoDbModule 中配置 context.Services.AddMongoDbContext 注入服务上下文和添加仓储
  "ConnectionStrings": {
    // 建议通过环境变量覆盖：ConnectionStrings__Default / ConnectionStrings__Chrelyonly
    "Default": "mongodb://",
    "Chrelyonly": "mongodb://"
  },
  "AuthServer": {
    "Authority": "https://localhost:44342",
    "RequireHttpsMetadata": true,
    "SwaggerClientId": "Radish_Swagger",
    // 建议通过环境变量覆盖：AuthServer__CertificatePassPhrase
    "CertificatePassPhrase": "CHANGE_ME_IN_ENV"
  },
  "StringEncryption": {
    // 建议通过环境变量覆盖：StringEncryption__DefaultPassPhrase
    "DefaultPassPhrase": "CHANGE_ME_IN_ENV"
  }
}
