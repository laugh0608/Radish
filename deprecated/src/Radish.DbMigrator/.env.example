# Copy this file to `.env` in the same directory and fill the values to align with Host.

# MongoDB connection strings (must match Host)
ConnectionStrings__Default=mongodb://
ConnectionStrings__Chrelyonly=mongodb://

# OpenIddict application RootUrl alignment is configured in appsettings.json for seeding.
# No env needed here unless you override via environment.

