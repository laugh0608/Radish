此文件解释 Visual Studio 如何创建项目。

以下为生成此项目的步骤:
- 新建 ASP\.NET 核心 Web API 项目。
- 更新项目文件以添加对前端项目的引用并设置 SPA 属性。
- 更新 `launchSettings.json` 以将 SPA 代理注册为启动程序集。
- 添加 `dockerfile` 以设置 docker 生成。
- 将项目添加到启动项目列表。
- 写入此文件。
