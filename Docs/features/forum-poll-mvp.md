# 论坛投票 MVP 设计方案

> 面向 **P3 首个候选功能：投票最小首版** 的实现方案。
>
> **版本**: v26.3.4
> **最后更新**: 2026.03.18
>
> 关联文档：
> [开发路线图](/development-plan) ·
> [当前进行中](/planning/current) ·
> [论坛应用功能说明](./forum-features.md) ·
> [社区主线验收清单](./community-m12-p0-acceptance.md)

---

## 当前实施进度

### 截至 2026-03-12 已完成

- 方案已确认采用“论坛帖子附带投票”路径，不拆独立投票 App。
- 后端第 1 步已完成：`PostPoll`、`PostPollOption`、`PostPollVote` 及相关 DTO / Vo 已落地。
- 后端第 2 步已完成：发帖接口已支持附带投票创建，帖子详情与列表已补最小投票摘要字段。
- 当前实现已通过 `Radish.Api` 构建验证，可作为后续接口与前端接入基线。

### 截至 2026-03-13 新增完成

- 第 3 步已完成：`PollController`、投票提交接口、按帖子查询投票结果接口已落地。
- 第 4 步已基本完成：欢迎 App 中论坛发帖弹窗已支持附带投票，帖子详情页已支持投票提交与结果展示。
- 最小后端回归测试已补充，覆盖首次投票成功、重复投票拦截、截止态拦截与非法选项拦截。
- `radish.client` 构建已通过，之前阻塞的 `PostDetail.tsx` 字段类型缺口已一并收口。
- 第 6 步已完成：帖子列表投票摘要已改为按页批量回填分类 / 标签 / 投票轻量摘要，不再逐条调用详情接口。
- 第 5 步持续推进：已补“发帖附带投票成功创建”“列表轻量 / 详情完整契约”“无效投票参数返回 400”回归。
- `Radish.Api.Forum.Poll.http` 已补论坛投票人工验收顺序，可串联“发帖附带投票 -> 列表识别 -> 详情查看 -> 提交投票 -> 重复投票拦截 -> 截止态拦截”。
- 当前下一步聚焦第 5 步：继续补更完整回归测试，并完成 MVP 验收收口。

### 截至 2026-03-14 新增完成

- 第 5 步已完成收口：补齐“短时截止投票可正常创建”的 UTC 时间口径回归，修复临近当前时间时被误判为已过期的问题。
- 相关投票服务与发帖服务的截止校验已统一按 UTC 比较，与 API 现有时间序列化口径保持一致。
- 手工联调已完成一轮冒烟验收：普通发帖、附带投票发帖、列表识别、详情展示、登录投票、重复投票拦截、未登录只读、截止态拦截均已通过。
- 论坛问答 MVP 已在同一阶段完成最小闭环，当前产品形态保持“普通帖 / 投票帖 / 问答帖”并行存在，但发帖入口仍维持“投票与问答互斥”的边界。
- 帖子列表卡片的展示口径已进一步统一：左侧标签区只保留“投票”类型标识，不再同时堆叠票数；票数统一放在右侧统计区展示。
- 当前结论已切换为：论坛投票 MVP 达到“可演示、可联调、可回归”的收口标准，可从当前主线移出，后续如继续推进则进入 `P3-ext` 扩展阶段。

### 截至 2026-03-17 新增完成

- `P3-ext` 首个轻扩展已落地：论坛列表新增“投票”视图，可直接筛出投票帖，不再只能依赖关键字搜索或混合列表识别。
- `Post/GetList` 已支持 `postType=poll`，保持与现有 `all / question` 视图同一入口，不额外新增投票列表接口。
- 前端继续维持“列表轻摘要、详情重交互”的边界：列表只负责筛选与识别，详情页的投票提交与结果展示链路保持不变。
- `Radish.Api.Forum.Poll.http` 已补“投票视图列表”请求，可用于串联本次扩展的最小专题回归。

### 截至 2026-03-18 新增完成

- `P3-ext` 第二个轻扩展已落地：投票视图新增“全部状态 / 进行中 / 已截止”筛选，不再只能在同一投票列表中混看全部状态。
- `Post/GetList` 已支持 `pollStatus=all / active / closed`，继续复用现有帖子列表分页、关键词与排序链路，不额外新增投票状态专用接口。
- 后端投票列表查询已统一按当前 UTC 时间判断截止态，手动关闭与截止时间到达都会归入“已截止”。
- 控制器与服务最小回归已补齐，覆盖 `pollStatus` 参数透传与“进行中 / 已截止”筛选结果。
- `P3-ext` 第三个轻扩展已落地：投票视图新增“票数优先”排序，当前 `poll` 视图只保留 `最新 / 票数` 两个最小排序入口。
- `Post/GetList` 已支持 `sortBy=votes`，排序口径为“置顶优先 -> 票数降序 -> 创建时间倒序”，继续复用现有列表入口，不额外新增投票专用排序接口。
- 控制器与服务最小回归已补齐，覆盖 `votes` 参数透传与票数排序结果。
- `P3-ext` 第四个轻扩展已落地：投票视图新增“即将截止优先”排序，当前 `poll` 视图已具备 `最新 / 票数 / 即将截止` 三个最小排序入口。
- `Post/GetList` 已支持 `sortBy=deadline`，排序口径为“置顶优先 -> 进行中且有截止时间的按截止时间升序 -> 长期有效 -> 已截止”，继续复用现有列表入口，不额外新增投票专用排序接口。
- 控制器与服务最小回归已补齐，覆盖 `deadline` 参数透传与即将截止排序结果。
- 真实联调补丁已补：SQLite 环境下点击投票“进行中”筛选时，`Nullable<DateTime>` 的 `HasValue/Value` 查询翻译会触发 SQL 语法错误；当前已改为 `null` 比较写法，恢复 `pollStatus=active` 可用性。
- `P3-ext` 第五个轻扩展已落地：投票详情页新增“结束投票”最小入口，仅发帖者可手动关闭进行中的投票。
- 新增 `Poll/Close` 接口，当前关闭口径为“仅发帖者可操作，关闭后立即归入已截止”，不扩展为重新开启、管理员代关或通知联动。
- 控制器与服务最小回归已补齐，覆盖“发帖者成功结束投票”“非作者被拒绝”“已截止投票不可重复关闭”。

---

## 目标

在现有论坛帖子主链路上增加一套**最小可用的投票能力**，满足以下目标：

- 用户可在发帖时附带一个单选投票。
- 用户可在帖子详情页查看投票、参与投票、查看结果。
- 帖子列表可识别“投票帖”，但不引入复杂交互。
- 尽量复用现有论坛发布、详情、权限与多租户链路，不新开独立 App。

本轮目标是“**小而快的闭环能力**”，不是一次性做完完整活动系统。

---

## 为什么先做投票

在 `抽奖 / 投票 / 问答` 三个候选方向中，投票最适合作为 `P3` 首个 MVP：

- **复用论坛主链路最多**：可直接挂在帖子上，复用发帖、详情、列表、评论与权限体系。
- **对现有模型冲击最小**：相比问答，不需要引入“问题 / 回答”双层内容模型。
- **业务风险最低**：相比抽奖，不涉及开奖公平性、奖品发放、审计与防刷。
- **更容易验收**：发帖、投票、查看结果三步即可形成闭环。

因此，本方案默认采用“**论坛帖子附带投票**”而非“独立投票应用”。

---

## MVP 范围

### 本轮包含

- 单选投票
- 发帖时附带投票
- 2 到 6 个投票选项
- 可选截止时间
- 帖子详情页投票与结果展示
- 已登录用户单人单票
- 帖子列表页显示投票标识与总票数

### 本轮不包含

- 多选投票
- 匿名投票
- 撤票 / 改票
- 投票评论区统计联动
- 通知系统联动
- 奖励 / 经验 / 萝卜币联动
- Console 投票管理页
- 独立投票 App

---

## 产品形态

### 位置选择

投票不做成独立应用，直接挂在论坛帖子中：

- **发布入口**：论坛发帖弹窗新增“附带投票”开关。
- **列表页**：帖子卡片显示“投票”徽标与票数摘要。
- 当前实现补充：数量型摘要统一收敛到卡片右侧统计区，左侧标签区仅承担帖子类型表达。
- **详情页**：正文下方、互动区上方插入投票卡片。

### 用户流

#### 发帖者

1. 打开发帖弹窗
2. 输入标题、正文、分类、标签
3. 开启“附带投票”
4. 填写投票问题、选项、截止时间
5. 发布帖子

#### 参与者

1. 在帖子列表识别投票帖
2. 打开帖子详情
3. 查看投票题目与选项
4. 选择一个选项并提交
5. 查看结果与自己所选项

---

## 业务规则

### 发帖规则

- 投票问题必填。
- 投票选项数量必须在 `2 ~ 6` 之间。
- 选项文本不能为空、不可重复。
- 截止时间可为空；为空表示长期有效，直到手动关闭或后续扩展阶段再补后台控制。
- MVP 阶段继续保持“投票帖”和“问答帖”互斥，不支持同一帖子同时附带投票与问答。

### 投票规则

- 仅登录用户可投票。
- 单个用户对单个投票只允许投一次。
- 已投票后不可重复投票。
- 已截止或已关闭时不可投票。
- 未登录用户可查看结果，但不能提交。

### 编辑与删除规则

- 带投票的帖子在 MVP 阶段**允许编辑标题 / 正文 / 分类 / 标签**。
- **不允许编辑投票问题与选项**，避免历史票数语义漂移。
- 删除帖子时，投票随帖子一并失效。
- MVP 不提供独立“删除投票”入口。

---

## 后端设计

### 数据模型

建议新增 3 个实体：

#### 1. `PostPoll`

表示一个帖子附带的投票主体。

建议字段：

- `PostId`
- `Question`
- `EndTime`
- `IsClosed`
- `TotalVoteCount`
- `TenantId`
- `IsDeleted`
- 审计字段

#### 2. `PostPollOption`

表示投票选项。

建议字段：

- `PollId`
- `OptionText`
- `SortOrder`
- `VoteCount`
- `TenantId`
- `IsDeleted`
- 审计字段

#### 3. `PostPollVote`

表示用户投票记录。

建议字段：

- `PollId`
- `PostId`
- `OptionId`
- `UserId`
- `UserName`
- `TenantId`
- `CreateTime`

### 模型约束

- `PostPoll`、`PostPollOption` 建议实现 `ITenantEntity`。
- `PostPoll`、`PostPollOption` 建议走软删除。
- `PostPollVote` 建议对 `(PollId, UserId)` 建唯一约束，保证单人单票。
- 投票写入时必须使用事务，同时更新：
  - `PostPoll.TotalVoteCount`
  - `PostPollOption.VoteCount`

### 与帖子关系

- 一个帖子最多挂一个投票。
- 投票归属于帖子，不单独对外暴露为独立内容类型。
- 帖子详情查询时附带投票聚合结果。

---

## ViewModel 与 DTO 设计

遵循当前项目 `Vo` 命名规范，建议新增：

### ViewModel

#### `PostPollVo`

- `VoPollId`
- `VoPostId`
- `VoQuestion`
- `VoEndTime`
- `VoIsClosed`
- `VoTotalVoteCount`
- `VoHasVoted`
- `VoSelectedOptionId`
- `VoOptions`

#### `PostPollOptionVo`

- `VoOptionId`
- `VoOptionText`
- `VoSortOrder`
- `VoVoteCount`
- `VoVotePercent`

#### `PollVoteResultVo`

- `VoPostId`
- `VoPoll`

### DTO

#### `CreatePollDto`

- `Question`
- `EndTime`
- `Options`

#### `PollOptionDto`

- `OptionText`
- `SortOrder`

#### `VotePollDto`

- `PostId`
- `OptionId`

### 对现有 DTO 的扩展

建议在 `PublishPostDto` 中新增可选字段：

```csharp
public CreatePollDto? Poll { get; set; }
```

`UpdatePostDto` 在 MVP 阶段先不扩展投票编辑字段。

---

## 接口设计

### 控制器划分

- 帖子主体继续由 `PostController` 承担。
- 投票动作建议新增 `PollController`，避免把帖子控制器继续堆大。

### 最小接口集

#### 1. 发布帖子（附带投票）

`POST /api/v1/Post/Publish`

- 继续复用现有发帖接口
- 请求中可选携带 `Poll`

#### 2. 获取帖子详情（包含投票）

`GET /api/v1/Post/GetById/{id}`

- 返回现有 `PostDetail`
- 若帖子带投票，则附带 `VoPoll`

#### 3. 提交投票

`POST /api/v1/Poll/Vote`

请求：

- `postId`
- `optionId`

返回：

- 最新 `PostPollVo`

#### 4. 按帖子获取投票详情

`GET /api/v1/Poll/GetByPostId?postId=`

用途：

- 前端局部刷新投票结果
- 详情页单独拉取或投票后刷新

### 错误语义

建议统一给出明确失败消息：

- 投票不存在
- 当前帖子未配置投票
- 你已经投过票
- 投票已截止
- 当前状态不允许投票
- 选项不存在

---

## Service / Repository 设计

### Service

建议新增：

- `IPostPollService`
- `PostPollService`

建议职责：

- 创建帖子投票
- 获取帖子投票详情
- 提交投票
- 计算投票结果与百分比

### Repository

优先策略：

- 先通过 `IBaseRepository<TEntity>` 与必要的仓储扩展完成
- 仅当投票聚合查询复杂时，再拆 `PostPollRepository`

### 事务边界

投票提交必须放在同一事务内完成以下操作：

1. 校验投票存在、未关闭、未截止
2. 校验用户未投票
3. 插入 `PostPollVote`
4. 更新 `PostPollOption.VoteCount`
5. 更新 `PostPoll.TotalVoteCount`

---

## 前端设计

### 发布入口

文件基线：

- `Frontend/radish.client/src/apps/forum/components/PublishPostModal.tsx`
- `Frontend/radish.client/src/types/forum.ts`
- `Frontend/radish.client/src/api/forum.ts`

建议新增：

- “附带投票”开关
- 投票问题输入框
- 动态选项编辑区
- 截止时间选择器

校验规则：

- 问题必填
- 至少 2 个选项
- 最多 6 个选项
- 选项不可重复

### 列表页

在帖子卡片中增加轻量摘要字段：

- `voHasPoll`
- `voPollTotalVoteCount`
- `voPollIsClosed`

展示策略：

- 显示“投票”徽标
- 票数统一显示在右侧统计区
- 不在列表页直接投票

### 详情页

文件基线：

- `Frontend/radish.client/src/apps/forum/components/PostDetail.tsx`

新增投票卡片：

- 标题 / 问题
- 选项列表
- 截止状态
- 总票数

交互状态：

- **未登录**：显示结果，投票按钮置灰
- **已登录未投票**：可选择一个选项并提交
- **已投票**：显示结果并高亮自己的选项
- **已截止**：只显示结果

### 前端类型扩展

建议在 `types/forum.ts` 中补充：

- `PostPollVo`
- `PostPollOptionVo`
- 扩展 `PostItem`
- 扩展 `PostDetail`
- 扩展 `PublishPostRequest`

---

## 接口返回建议

### 帖子列表

为了避免 N+1，帖子列表不返回完整投票明细，只返回轻量摘要：

- `VoHasPoll`
- `VoPollTotalVoteCount`
- `VoPollIsClosed`

### 帖子详情

详情页返回完整 `VoPoll`：

- 问题
- 选项
- 票数
- 百分比
- 当前用户投票状态

### 为什么这样分层

- 列表页保持轻量，避免每个帖子都拉全量选项。
- 详情页承担完整交互，符合当前论坛架构。

---

## 权限与安全

### 权限边界

- 发帖权限继续复用现有 `PostController.Publish` 的发布权限拦截。
- 投票权限默认跟随“登录用户可参与”规则，不额外引入新的权限族。
- 管理员在 MVP 阶段不提供特殊投票管理操作。

### 风险点

#### 1. 并发重复投票

必须依赖：

- 数据库唯一约束
- 事务

不能只靠前端按钮置灰。

#### 2. N+1 查询

帖子列表不能对每条帖子单独查投票详情。

#### 3. 选项编辑导致历史漂移

因此 MVP 阶段直接禁掉投票编辑。

#### 4. 租户隔离

投票实体与投票记录必须带 `TenantId`，跟随帖子租户口径。

---

## 验收标准

满足以下条件即可认为投票 MVP 可进入验收：

- 可发布带投票的帖子
- 帖子详情可正确展示投票
- 登录用户可成功投票
- 重复投票会被拦截
- 截止后不能继续投票
- 帖子列表可识别投票帖
- 前后端字段口径一致，无匿名对象返回

### 当前验收结论（2026-03-14）

- 已完成最小自动回归与一轮手工冒烟验收。
- 截止时间临近当前时刻的误判问题已修复，当前口径统一为“输入按前端本地时间，接口与存储按 UTC，展示按用户当前展示时区”。
- 本轮 MVP 通过，不再继续把多选、匿名、撤票或 Console 管理能力纳入当前阻塞项。
- 列表卡片中投票与问答的数量展示口径已统一，避免再出现左侧标签和右侧统计重复表达同一数值。

### 变更回归记录（2026-03-17：投票视图筛选）

- 记录日期：2026-03-17
- 记录人：Codex
- 变更范围：论坛投票 `P3-ext` 首个轻扩展，新增列表“投票”视图与 `postType=poll` 契约

#### 变更摘要

- 新增论坛列表“投票”视图按钮，支持直接筛选投票帖
- 扩展 `Post/GetList` 的 `postType`，新增 `poll` 取值
- 补充控制器 / 服务最小回归与 `Forum.Poll.http` 验证入口

#### 影响专题

- 论坛投票
- 论坛帖子列表

#### 自动化执行

- `npm run validate:baseline:quick`：通过
- `dotnet test Radish.Api.Tests --filter "FullyQualifiedName~PostControllerTest|FullyQualifiedName~PostServiceTest"`：通过
- `npm run validate:baseline:host`：未执行

#### 专题回归

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http`：已按“发帖附带投票 -> 投票视图列表 -> 详情查看 -> 提交投票 -> 重复投票拦截”顺序完成一轮手动联调
- 本文档“最小人工验收顺序”：已同步加入“切换到投票视图确认列表筛选”动作

#### 人工验收

- 执行情况：已执行
- 摘要：
  - 已串联发帖附带投票、列表“投票”视图筛选、详情投票与重复投票拦截
  - 当前手动联调未发现新增回归或契约断裂

#### 结论

- 当前改动已通过自动化与一轮真实手动联调，可作为 `P3-ext` 的首个最小扩展继续推进

#### 风险 / 后置项

- 当前投票视图仅补列表筛选，尚未增加投票专用排序、关闭态筛选或更重的交互增强

### 变更回归记录（2026-03-18：投票状态筛选）

- 记录日期：2026-03-18
- 记录人：Codex
- 变更范围：论坛投票 `P3-ext` 第二个轻扩展，新增列表投票状态筛选与 `pollStatus` 契约

#### 变更摘要

- 新增投票列表状态筛选按钮，支持 `全部状态 / 进行中 / 已截止`
- 扩展 `Post/GetList` 的 `pollStatus`，新增 `active / closed` 取值
- 补充控制器 / 服务最小回归，覆盖投票状态参数透传与关闭态筛选

#### 影响专题

- 论坛投票
- 论坛帖子列表

#### 自动化执行

- `npm run type-check --workspace=radish.client`：通过
- `dotnet test Radish.Api.Tests --filter "FullyQualifiedName~PostControllerTest|FullyQualifiedName~PostServiceTest"`：通过（24 个测试）
- `npm run validate:baseline:host`：未执行

#### 专题回归

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http`：已按“投票视图 -> 投票状态筛选 -> 详情查看 -> 提交投票”顺序完成一轮真实联调
- 本文档“最小人工验收顺序”：已纳入“切换投票状态筛选确认进行中 / 已截止结果”动作

#### 人工验收

- 执行情况：已执行
- 摘要：
  - 已确认 `全部状态 / 进行中 / 已截止` 三态切换口径正常
  - SQLite 环境下点击“进行中”不再触发 SQL 语法错误

#### 结论

- 当前改动已完成代码与回归入口收口，可作为 `P3-ext` 的第二个最小扩展继续推进

#### 风险 / 后置项

- 当前仅补投票状态筛选，尚未增加投票专用排序、关闭时间优先或更复杂的交互增强

### 变更回归记录（2026-03-18：投票票数排序）

- 记录日期：2026-03-18
- 记录人：Codex
- 变更范围：论坛投票 `P3-ext` 第三个轻扩展，新增投票视图 `票数优先` 排序与 `sortBy=votes` 契约

#### 变更摘要

- 新增投票列表“票数”排序按钮，当前 `poll` 视图只保留 `最新 / 票数`
- 扩展 `Post/GetList` 的 `sortBy`，新增 `votes` 取值用于投票列表
- 补充控制器 / 服务最小回归，覆盖 `votes` 参数透传与票数排序结果

#### 影响专题

- 论坛投票
- 论坛帖子列表

#### 自动化执行

- `npm run type-check --workspace=radish.client`：通过
- `dotnet test Radish.Api.Tests --filter "FullyQualifiedName~PostControllerTest|FullyQualifiedName~PostServiceTest"`：通过（26 个测试）
- `npm run validate:baseline:host`：未执行

#### 专题回归

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http`：已按“投票视图 -> 投票状态筛选 -> 票数排序 -> 详情查看 -> 提交投票”顺序完成一轮真实联调
- 本文档“最小人工验收顺序”：已纳入“切换票数排序确认高票帖子靠前”的动作

#### 人工验收

- 执行情况：已执行
- 摘要：
  - 已确认高票帖子在“票数”排序下优先展示
  - 状态筛选与票数排序组合切换未发现回归

#### 结论

- 当前改动已完成代码与回归入口收口，可作为 `P3-ext` 的第三个最小扩展继续推进

#### 风险 / 后置项

- 当前仅补票数优先排序，尚未增加“即将截止优先”或更复杂的投票专用排序策略

### 变更回归记录（2026-03-18：投票即将截止排序）

- 记录日期：2026-03-18
- 记录人：Codex
- 变更范围：论坛投票 `P3-ext` 第四个轻扩展，新增投票视图“即将截止优先”排序与 `sortBy=deadline` 契约

#### 变更摘要

- 新增投票列表“即将截止”排序按钮，当前 `poll` 视图具备 `最新 / 票数 / 即将截止`
- 扩展 `Post/GetList` 的 `sortBy`，新增 `deadline` 取值用于投票列表
- 补充控制器 / 服务最小回归，覆盖 `deadline` 参数透传与即将截止排序结果

#### 影响专题

- 论坛投票
- 论坛帖子列表

#### 自动化执行

- `npm run type-check --workspace=radish.client`：通过
- `dotnet test Radish.Api.Tests --filter "FullyQualifiedName~PostControllerTest|FullyQualifiedName~PostServiceTest"`：通过（28 个测试）
- `npm run validate:baseline:host`：未执行

#### 专题回归

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http`：已按“投票视图 -> 投票状态筛选 -> 票数排序 -> 即将截止排序 -> 详情查看 -> 提交投票”顺序完成一轮真实联调
- 本文档“最小人工验收顺序”：已纳入“切换即将截止排序确认临近截止样例靠前”的动作

#### 人工验收

- 执行情况：已执行
- 摘要：
  - 已确认进行中且临近截止的样例靠前，长期有效样例其后，已截止样例在最后
  - `最新 / 票数 / 即将截止` 三种排序切换整体正常

#### 结论

- 当前改动已完成代码与回归入口收口，可作为 `P3-ext` 的第四个最小扩展继续推进

#### 风险 / 后置项

- 当前仅补“即将截止优先”排序，尚未增加更复杂的混合排序或活动化排序策略

### 变更回归记录（2026-03-18：投票关闭管理）

- 记录日期：2026-03-18
- 记录人：Codex
- 变更范围：论坛投票 `P3-ext` 第五个轻扩展，新增投票详情页“结束投票”入口与 `Poll/Close` 契约

#### 变更摘要

- 新增 `Poll/Close` 接口，仅允许发帖者手动结束自己进行中的投票
- 欢迎 App 论坛帖子详情页已补“结束投票”按钮，关闭后原位刷新为“已截止”
- 补充控制器 / 服务最小回归，覆盖关闭成功、非作者拒绝与已截止投票重复关闭

#### 影响专题

- 论坛投票
- 论坛帖子详情

#### 自动化执行

- `npm run type-check --workspace=radish.client`：通过
- `dotnet test Radish.Api.Tests/Radish.Api.Tests.csproj --no-build --filter "FullyQualifiedName~Radish.Api.Tests.Services.PostPollServiceTest|FullyQualifiedName~Radish.Api.Tests.Controllers.PollControllerTest"`：通过（10 个测试；因本机运行中的 `Radish.Api` / `Radish.Auth` 占用默认输出目录，实际复用临时输出目录执行）
- `npm run validate:baseline:host`：未执行

#### 专题回归

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http`：已补“发帖者结束投票”与关闭后重新获取详情请求
- 本文档“最小人工验收顺序”：本轮已补入“发帖者手动结束投票并确认状态切换”的动作

#### 人工验收

- 执行情况：已执行
- 摘要：
  - 已确认发帖者可在详情页手动结束投票，关闭后状态立即切换为“已截止”
  - 关闭后回查投票详情、状态筛选与截止态拦截均正常，未发现新增回归

#### 结论

- 当前改动已完成代码与回归入口收口，可作为 `P3-ext` 的第五个最小扩展继续推进

#### 风险 / 后置项

- 当前仅补发帖者手动关闭，不扩展重新开启、管理员代关、关闭通知或奖励联动

---

## 最小测试清单

### 后端

- 创建带投票帖子成功
- 选项少于 2 个时失败
- 首次投票成功
- 重复投票失败
- 截止后投票失败
- 不存在选项时失败
- 跨租户不可投

### 前端

- 发布弹窗可创建投票
- 帖子详情能显示投票结果
- 未登录用户只能看不能投
- 已投票状态刷新后可回显
- 列表页投票摘要显示正常

### 人工验收模板（样板）

> 结构遵循：[人工验收模板](/guide/manual-acceptance-template)

#### 适用改动范围

- 投票模型、投票 DTO / Vo、发帖附带投票改动后
- 投票提交、截止时间判断、结果展示改动后
- 帖子列表投票摘要、详情页投票交互改动后

#### 前置条件

- 已启动 `Radish.Api`、`Radish.Auth`、`Radish.Gateway`
- 已通过 `Radish.Api.AuthFlow.http` 获取 `access_token`
- 已准备一个登录用户账号；如需验证“未登录只读”，需同时准备无 token 场景
- 已能打开欢迎 App 中的论坛列表与帖子详情

#### 联调资产

- `Radish.Api.Tests/HttpTest/Radish.Api.Forum.Poll.http` 已补论坛投票验收段，便于手工串联最小闭环。

#### 执行顺序建议

建议把论坛投票回归拆成三层执行，避免把自动化验证、接口串联和欢迎 App 观察混成一轮“大而全”的联调：

1. 自动化层：优先执行前端 `type-check` 与投票相关后端测试，先确认契约、参数校验和最小业务逻辑没有回退。
2. `HttpTest` 层：执行 `Radish.Api.Forum.Poll.http`，至少串联 `postType=poll`、`pollStatus=active / closed`、`sortBy=votes`、`sortBy=deadline`、`Poll/Close` 这几类入口。
3. 人工联调层：回到欢迎 App 的论坛列表与详情页，确认作者态按钮显隐、状态切换、筛选与排序组合切换是否符合真实交互预期。

#### 真实环境注意事项

- Windows 本机如果正运行 `Radish.Api`、`Radish.Auth` 或调试器相关进程，默认 `bin\Debug` 输出目录可能被占用，导致 `dotnet build` / `dotnet test` 因 DLL 锁定失败。
- 这种场景下，优先停掉宿主后再跑默认验证；如果只是做必要的编译 / 测试确认，也可以改用隔离输出目录执行，避免影响正在运行的联调环境。
- 自动化验证与真实联调尽量分开，不要在同一份默认输出目录上来回覆盖，否则很容易出现“代码没问题，但验证环境互相干扰”的假失败。

#### 最小人工验收顺序

1. 执行 `Radish.Api.Forum.Poll.http`，发布一篇带投票的帖子，确认发帖成功且详情能返回投票主体与选项。
2. 返回论坛列表，确认该帖子能被识别为投票帖；再切换到“投票”视图，确认该帖子能被正确筛出，且列表摘要与详情展示口径一致。
3. 在“投票”视图中切换 `全部状态 / 进行中 / 已截止`，确认未截止样例只出现在“进行中”，手动关闭或已过截止时间的样例只出现在“已截止”。
4. 切换到“票数”排序，确认高票帖子优先展示；若票数相同，则较新的帖子排在前面。
5. 切换到“即将截止”排序，确认进行中且临近截止的样例排在前面，长期有效排在其后，已截止样例排在最后。
6. 以登录用户提交一次投票，确认首次投票成功，刷新详情后已投状态与统计结果可回显。
7. 再次提交同一投票，确认返回“重复投票”类失败语义，而不是静默成功。
8. 以发帖者身份在详情页执行“结束投票”，确认按钮只对作者可见，执行后投票立即切换为“已截止”且不再允许提交。
9. 切换到未登录态访问帖子详情，确认可以查看投票结果但不能提交投票。
10. 使用一个已过期或即将过期的投票样例验证截止态，确认截止后提交会被稳定拦截。

#### 预期结果

- 发帖附带投票、列表状态筛选、票数排序、即将截止排序、详情展示、首次投票、重复投票拦截、作者手动结束投票、截止态拦截主链路可完成。
- 列表摘要与详情展示一致，不出现列表能识别但详情缺字段，或详情正常但列表无摘要的断链。
- 未登录用户保持只读，不误放开投票入口。

#### 可跳过项

- 仅后端截止时间或参数校验调整时，可跳过欢迎 App 列表样式观察，但至少保留详情提交与失败语义验证。
- 仅前端展示样式调整时，可跳过过期投票边界验证，但至少保留一次真实投票与刷新回显。

#### 结论记录格式

```md
- 验收日期：2026-03-17
- 验收人：<name>
- 验收范围：论坛投票 MVP
- 执行入口：Radish.Api.Forum.Poll.http + 欢迎 App 论坛
- 结果：通过 / 阻塞
- 备注：<如有异常，记录帖子 ID、投票截止时间、账号状态与现象>
```

---

## 后续扩展方向

若 MVP 通过并继续推进，可进入 `P3-ext`：

- 多选投票
- 匿名投票
- 撤票 / 改票
- 投票关闭管理
- 通知联动
- 排行榜 / 活动页
- Console 管理页

---

## 结论

本方案选择“**论坛帖子附带投票**”作为第一版实现路径，核心原因是：

- 与现有论坛主链最贴合
- 复用现有发帖与详情体系
- 体量可控
- 风险小、验收快

如果要在 `P3` 三个候选中先落一个最小首版，投票是当前最合适的切入点。
