# 2026 年 8 月开发日志

> 当前主线为 `F4-R 家族 UI 统一接入与产品视觉重构`。C-1B 的六个 R1、`R2-C03` 与 `R2-P03 / Public 只读详情变体` 已形成设计—实现—Gateway 运行态闭环；下一顺位为 `R2-W02 Private 仪表 / 任务侧栏` 设计前审计。

## 本月摘要

- `2026-08-03` 完成 family-ui `v26.7.3` 破坏性基线升级：完整复核新版规范、token 与参考索引，确认 27 张参考图仅迁址且 Git blob 全部 `R100` 相同；同步 CSS / JSON 上游副本，新增 `text-on-brand`，并在 Radish 项目主题层显式保留 `guofeng` 胭脂品牌和四主题品牌前景。现有品牌 / 操作变量使用差异进入 `R1-F01` 代表设计裁决，本批未机械替换页面样式，也未启动服务或执行真实 smoke。
- `2026-08-03` 创建唯一活动设计源 `radish-web-family-ui-v1.pen`，以主版本号管理结构升级，普通迭代由 Git 留存；原四个大型 `.pen` 保持原样并转为只读留档。新文件已建立索引、可复用组件和 `R1-F01`，覆盖四主题矩阵、品牌 / 操作语义、控件、状态槽与 PC / 390px mobile 壳层。
- `2026-08-03` 形成 `R1-P01` 两轮 Public 内容流评审稿并完成独立 PC、390px mobile、关键状态和标题约束治理，但两轮均未通过业务页面审核。第二轮已分开分类、`#标签` 与结构化状态，仍因帖子占据绝大多数篇幅、全宽分类浏览笨重、构图偏离参考 `13 / 16 / 18 / 27` 而被否决；现有稿只由 Git 留档，不进入实现。国风灰玉 `#5d6c57` 与墨蓝操作 `#435c74` 的颜色目标继续保留，当前运行时旧胭脂值不提前修改。
- `2026-08-04` 完成参考 `13 / 16 / 18 / 27` 结构转译、公开能力审计、低保真候选比较与多轮视觉重校准，最终确认 `R1-P01 / 社区发现 / PC 1440`。该稿固定现代自然紧凑表面、非对称主次、连续扫描行、灰玉品牌、墨蓝操作和国风暖白，并将公共频道、成员公开动态、跨帖神评、帖子与问答并置为“社区正在发生”。
- `2026-08-04` 清理活动设计源中的旧 PC / mobile 和全部失败研究，只保留唯一正式 PC、`8` 个必要组件母版与主题变量；长期规范新增 R1-P01 视觉继承卡和获批后删除废稿规则。mobile 与统一公开读模型仍待下一轮，运行时代码、token 和 API 未修改。
- `2026-08-05` 在唯一活动设计源中完成并确认 `R1-P01 / 社区发现 / Mobile 390` 正式代表页：搜索与真实内容前置，参与动作进入精选讨论，社区脉搏、贡献者和知识主题分别嵌入首屏、中段与流后；首轮反馈后，“社区正在发生”从异色列表拼接重构为墨蓝焦点事件、连续编号轨道和嵌入式贡献者人物节点。没有缩放 PC 或把右栏机械堆叠；画板高度 `1547px`，节点检查无裁切、溢出或失效图标，`2x` 视觉自检通过，用户已确认。
- `2026-08-05` 为五类“社区正在发生”item 固定统一公开读模型：频道摘要显式 `DiscoverVisibility=Summary`、成员动态首批只来自公开 Wiki 首次发布、跨帖神评联动当前 Comment / Post 资格，帖子与问答复用既有公开契约；排序采用 snapshot cutoff 与稳定 keyset 游标，社区脉搏只统计公开频道和近 `24` 小时可重算聚合，首批完整 feed `no-store` 并在跨源失败时关闭。该设计批当时尚未修改运行时代码、迁移、token 或 API。
- `2026-08-05` 完成 `R1-P01` 成组实现：Channel 增加默认 Hidden 的匿名摘要字段、版本和 migration，Console 增加独立 `view / manage` 权限、精确租户治理与 append-only 事件；`PublicDiscover` 以当前权威数据合并五类 item，提供 snapshot 游标、纯文本白名单、`no-store` 和整流失败关闭。`@radish/http` 与 `/discover` 已改为只消费统一 feed，页面使用首条焦点事件、连续编号轨道、嵌入式贡献者和非对称洞察区。后端专题 `57`、Client 全量 `489`、Console 全量 `61` 项测试及前后端构建通过；本批未启动服务，PostgreSQL 条件用例与 Gateway 真实截图仍待阶段验收。
- `2026-08-05` 完成 `R1-P01` 阶段性运行态验收：应用 migration 后 Gateway、API、Auth 宿主检查通过；匿名与种子管理员登录回流、PC `1440 × 1000`、mobile `390 × 844`、`zh / en`、`default / guofeng`、真实链接、复制反馈和 Console 治理只读路径通过，页面无横向溢出。运行态同时修正后端 `long` 字符串序列化与前端计数 `number` 假设不一致导致的 i18n 资源键回显；Console 实际状态写入未在浏览器中提交，幂等、版本冲突、领域事件与写入契约继续由后端定向测试覆盖。
- `2026-08-05` 完成 `R1-P02 / Public 详情与互动` 代码事实与设计边界审计：核对 `/forum/post/:id` 的七类受控 intent、帖子 / 轻回应 / 评论 / 回答能力、身份权限、局部失败和 PC / mobile 当前结构；确认现有主要视觉债为重复参与入口、解释型侧栏、卡片套卡片和 mobile 辅助区长尾。代表设计固定为连续文章阅读面、唯一帖子操作带、条件回答区、紧凑轻回应 / 评论流和线程索引；现有 API、权限与写入边界不变，`2292` 行详情容器在视觉实现前按真实领域区块拆分。
- `2026-08-08` 完成并关闭 `R1-P02`：正式帖子详情落地 PC 三栏阅读面与 Mobile 单列任务流，普通读者承接帖子 / 回帖点赞、reaction、赞赏和两级回帖；`PublicForumDetail.tsx` 从 `2292` 行收敛到 `1500` 行。Client `491` 项、Lint、production build 与 Gateway PC / mobile、双语、代表主题验收通过。
- `2026-08-08` 完成并关闭 `R1-A01`：先闭合 Author Revision 关系授权、终态证据、写响应证据、Apply 基准版本 CAS 与附件 ACL，再落地标题 / Markdown 正文主轴、PC context rail 和 Mobile Bottom Sheet。普通 Owner、Published 正式版本、`Editing` 共享草稿与 Accepted Editor 的 Gateway PC / mobile 验收通过。
- `2026-08-08` 完成并关闭 `R1-W01`：ChatMessage 举报 ACL / LongId、失败重试幂等与历史错误契约先通过能力门禁，随后落地连续会话列表 / 消息主轴 / 按需成员上下文、搜索与成员互斥、紧凑 Pin 和 Mobile 单任务流；Gateway Direct 覆盖消息、引用、图片、Reaction、Pin、已读边界和无横向溢出。
- `2026-08-08` 完成并关闭 `R1-C01`：Console 订单页落地 PC 单行薄表格与按需 inspector、Mobile 三列订单行 / 筛选层 / 全屏详情，闭合只读 Operator、重试 `409`、备注冲突、详情不可用、列表 stale、空结果、LongId URL 与资源回跳；Console `66 / 66`、Lint、strict type-check、production build 与 Gateway PC / mobile 验收通过。
- `2026-08-09` 完成 `R1-C02` 设计前静态门禁：确认 Case / Appeal、六类目标、权限、版本、幂等、结构化错误和 Main / Chat 事务能力足够；同时发现 Console 仍缺 PostAnswer 筛选 / 双语 / Revision 前置校验、案件 URL / mobile 全屏任务、stale / unavailable 和 View-only 权限表面。现行内容治理说明已同步第六目标，原 F4-I / F4-J 五类矩阵保留历史完成事实；本批未修改 Pencil、运行时代码、API、数据库或权限。
- `2026-08-09` 完成 `R1-C02` 前端能力门禁：Console 补齐 PostAnswer 筛选、双语和 Restrict Revision 前置校验；案件选择、筛选与分页进入 URL，Case / Appeal mobile 进入隐藏全局导航的全屏任务；队列 stale、详情 unavailable / stale 和 View-only 写入表面均按权威读取失败关闭。Console `73 / 73` 测试、Lint、strict type-check、production build 与变更卫生检查通过；未修改 Pencil、API、数据库、权限、LongId、幂等或事务边界，未启动服务或浏览器。
- `2026-08-09` 完成并确认 `R1-C02` 正式代表设计：PC `1440 × 900` 使用案件队列—受权证据—决定边界三段治理桌，Mobile `390 × 844` 使用连续案件队列、Console 五项真实入口，以及保留顶部品牌栏、隐藏五项底部导航的单案件全屏任务；关键状态覆盖 View-only、Reviewer 无 Action、`409`、stale / unavailable、筛选空结果和申诉 / Chat 边界。四个顶层画板均无 placeholder、裁切或溢出；该设计随后进入正式代码实现，设计批未启动服务或浏览器。
- `2026-08-09` 完成 `R1-C02` 正式成组实现：PC 落地队列—证据—决定三段治理桌，Mobile 落地连续三列案件队列、按需 `BottomSheet` 筛选，以及保留 Console 品牌栏、隐藏五项底部导航的单任务详情；既有权限、URL、LongId、幂等、结构化错误和 Main / Chat 事务边界保持不变。Console `75 / 75`、Lint、strict type-check、production build 与变更卫生检查通过；未启动服务或浏览器。
- `2026-08-09` 获授权完成 `R1-C02` Gateway 运行态验收：PC `1440 × 900` 三段治理桌与 Mobile `390 × 844` 三列队列、按需筛选、五项胶囊导航和保留品牌栏的全屏任务均无横向溢出；真实管理员读取、筛选空结果、申诉空队列和详情 `404` / unavailable 失败关闭通过。验收同时修正 Mobile 列表通用工具栏、未选详情占位和刷新图标过小三项壳层偏差；重启后的干净浏览器标签控制台为 `0 error / 0 warning`。权限、`409` 与 stale 继续由定向自动化分层守卫，未触发任何治理写操作或新增临时业务数据；临时 OIDC 会话精确清零，六库完整性通过，服务端口与浏览器标签关闭。`R1-C02` 正式关闭，下一步进入 `R2-C03` 设计前审计。
- `2026-08-09` 完成 `R2-C03` 设计前代码事实与能力覆盖审计：确认该类型继续复用 `AdminLayout`、Console 五项真实入口和 R1-C01 / C02，只需 R2 级权限矩阵、设置分区、危险确认与 Mobile 只读 / Low 风险差异；同时识别出只读矩阵路由不可达、内建角色无权威保护、授权版本 / 事务不可靠、系统设置冲突被包为 500、配置与审计未共同提交，以及 Settings / Profile 真实能力口径偏旧。下一步先成组关闭代码能力门禁；本批未修改 Pencil、运行时代码、API、数据库或权限，未启动服务或浏览器。
- `2026-08-09` 完成并关闭 `R2-C03`：服务端建立内建 `System / Admin` 保护、角色命令与唯一性、权限聚合单调版本 / 原子 CAS，以及系统设置结构化 `400 / 409`、配置—审计共同提交和显式 Medium 确认；唯一活动 `.pen` 的 PC 权限矩阵 / 设置与 Mobile 角色目录 / 只读权限 / Low 设置三个独立任务流获确认。正式 Console 落地只读 Operator、dirty / 离开保护、`409` 草稿、权限 key 含义和 Mobile Medium PC-only；Console `83 / 83` 与 Gateway PC `1440 × 900` / Mobile `390 × 844` 验收通过，未保存角色授权或设置，临时 OIDC 增量已清理。
- `2026-08-09` 完成 `R2-P03` readiness 与第一批能力门禁：Docs、Shop、Profile、Legal 继续继承 `R1-P02 / R1-F01`，不复制四个完整路由；正式商品详情复用既有 `Product` 举报，公开主页以主资料权威裁决 `404 / unavailable`，统计失败局部化且不伪造粉丝数。Client `512 / 512`、Lint、type-check 与 production build 通过。
- `2026-08-09` 经确认新增窄能力门禁：公开资料只投影当前等级 / 等级名；商品评价固定 Completed 订单资格、每用户每商品一条、`1..5` 星、可选 `500` 字评论、唯一约束、数据库聚合和单调 CAS。`ProductReview` 作为第七类受控目标复用既有治理 / 申诉权限、事务与 Console 工作台；后端 `1220 passed / 39 skipped`，HTTP `36 / 36`、Client `514 / 514`、Console `83 / 83` 及静态构建通过。
- `2026-08-09` 完成并确认 `R2-P03` 局部代表设计：三个独立 PC、三个独立 Mobile 和两块关键状态板覆盖长内容、商品、公开主页、真实零评价、资格、dirty、`409`、unavailable / stale 与治理 / 申诉。七轮反馈收紧 PC 外距与卡间距，修正主按钮对比度和 `carrot` 萝卜币图标，固定 Mobile 顶部大图电商顺序，并将公开主页按昵称、`用户名#公开ID`、签名、等级 / 等级名、加入时间及右侧统计 / 操作分区；八板无 placeholder、裁切、塌陷或横向溢出。下一开发日进入正式商品详情与公开主页实现。
- `2026-08-10` 完成并关闭 `R2-P03`：正式商品详情接入综合评分、五星分布、稳定分页、Completed 资格、本人评价 CRUD、dirty、CAS `409` 草稿保留、unavailable / stale 与 `ProductReview` 举报；公开主页接入昵称 / `用户名#公开ID`、真实签名空态、当前等级 / 等级名、加入时间和独立权威统计。DbMigrate 补齐本地商品评价迁移后，Gateway 以匿名和种子管理员覆盖 PC `1440 × 900`、Mobile `390 × 844`、零评价、临时 Completed 资格、创建 / 编辑 / 删除、登录回跳、dirty、真实 CAS 冲突、举报弹窗、双语 / 主题和无横向溢出；运行态修正受控 `intent=review` 回跳与错误本地化，新页签稳定态 `0 warning / 0 error`，临时订单与评价精确清零。Client `518 / 518`、type-check、Lint 与 production build 通过；未修改 Pencil。

## 当前结论

- 上游灰玉仍只是通用参考默认；Radish 已独立确认灰玉、墨蓝与国风暖白为 `R1-P01` 产品方向。页面已使用现有灰玉 / 墨蓝语义 token，全局 `guofeng` 品牌 token 因影响全部页面族保留到共享主题批，不能把局部落地描述为全站换色。
- 既有公开 head、canonical、分享路径、四主题权益和业务写入口保持专题契约；`R1-P02` 复用既有点赞、reaction、赞赏与回帖写入，没有建立第二套接口或路由真相源。
- `/discover`、帖子详情、Author 编辑、Private 消息、Console 订单、Console 案件治理与 Console 设置 / 权限矩阵均已完成设计、实现和运行态闭环；后续 R3 页面按对应代表基线继承，不逐路由复制完整画板。
- 设计轨后续只编辑 `radish-web-family-ui-v1.pen`；获批页面保留唯一代表稿，失败方案由 Git 追溯，不新建并行 `.pen`。任何 `.pen` 修改仍需当前任务明确授权。
- `R2-P03` 已把冻结的商品评价和公开等级契约接入正式商品详情 / 公开主页，并完成 Gateway PC / mobile 运行态闭环；评价媒体、回复、有用、独立评价中心和公开经验详情继续后置。
- 下一顺位进入 `R2-W02 Private 仪表 / 任务侧栏` 设计前代码事实与能力覆盖审计；先判断 `R1-F01 / R1-W01` 继承和窄代码门禁，不提前修改 Pencil 或推进 R3 页面。

`2026-08-10` R2-P03 页面实现与 Gateway 验收证据见[正式实现记录](/records/f4-r-r2-p03-public-read-only-detail-variants-implementation-2026-08-10)；`2026-08-09` 全部提交与代码—文档反查见[日终提交回顾与文档审阅](/records/f4-day-end-doc-review-2026-08-09)。R2-C03 与 R2-P03 的其余实现 / 设计结论见对应[记录索引](/records/)；此前日终结论见 [2026-08-08](/records/f4-day-end-doc-review-2026-08-08)、[2026-08-04](/records/f4-day-end-doc-review-2026-08-04)和 [2026-08-03](/records/f4-day-end-doc-review-2026-08-03)。
