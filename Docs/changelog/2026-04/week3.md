# 2026-04 第三周 (04-13 ~ 04-19)

## 2026-04-16 (周四)

### 公开壳层规划与验收口径收口

- **公开壳层高层真相源当前已完成一轮对齐**：默认经验榜单规范路径、`/discover -> /leaderboard -> /u/:id -> 返回` 的真实联调结果，以及公开商城详情来源返回口径，当前都已同步回写到 `planning/current`、专题验收清单与公开壳层总基线记录中。
- **公开内容壳层默认增量方向当前已进一步收束**：截至 `2026-04-16`，`Phase 2-2` 当前不再优先回头扩桌面工作台能力，而是明确把后续增量收口到“真实联调复核、来源返回稳定性观察与高价值只读阅读增强”。

### `/discover` 首屏信息层级增强

- **公开社区分发页当前已完成首轮信息层级重排**：forum / docs / leaderboard / shop 四个入口当前已在 `/discover` 首屏形成更清晰的摘要卡与导览卡层次，不再停留在“只有入口名 + 一跳直达”的弱导览状态。
- **公开入口导航、forum 推荐、docs 推荐、榜单快捷入口与商城推荐的首屏顺序当前已完成一轮真实联调收口**：移动端下横向撑破、区块顺序错位与卡片间距不一致等问题均已在本批次修正，公开分发页首屏节奏已回到统一口径。

### 公开头部共享基线统一

- **公开内容壳层窄屏头部当前已统一到共享组件**：forum、个人公开页等入口已改为复用同一套 `PublicShellHeader`，移动端下不再继续保留专题各自分叉的双行旧头部。
- **移动端头部当前已收口为更稳定的单行表达**：品牌字、回流按钮与次级动作在窄屏下已完成一轮实际样式修正，不再出现“有的页面单行、有的页面两行、还有个别页面错位”的分叉状态。

### `/discover` 摘要卡区块联动增强

- **摘要卡当前已从“整卡直接跳专题”改为双动作节奏**：主区域优先平滑滚动到本页对应区块，底部保留“直接进入公开页”动作；forum / docs / leaderboard / shop 四张摘要卡也会同步展示 ready / loading / error / empty 状态。
- **摘要卡与真实内容区的映射当前已重新挂正**：forum / docs / leaderboard / shop 四个区块的定位 `ref` 已回到各自真实 section，公开分发页不再出现“点 forum 摘要却滚到公开入口导航区”这类错位问题。

### `/discover` 区块来源回记

- **公开社区分发页当前已接通 `section` 级来源回记**：摘要卡预览、区块内“查看全部”和内容跳转会记住最近一次公开分发区块，并写回 `/discover?section=*` 这一层公开路由状态。
- **公开专题页回到“社区发现”时当前会优先回原区块**：forum / docs / leaderboard / shop / `u/:id` 顶部继续保留统一“社区发现”轻入口，但若当前阅读链路来自 `/discover` 的某个已记录区块，回流时会优先回到对应区块而不是强制落回页首。

### 文档同步与最小验证

- **专题验收清单当前已同步真实行为**：`discover-public-acceptance` 已补入“摘要卡主区域滚动预览 + 直接进入公开页”与“`section` 来源回记后回流优先落原区块”的人工验收口径。
- **本轮最小前端验证已通过**：`npm run type-check --workspace=radish.client` 与 `npm run build --workspace=radish.client` 当前均已通过；构建验证在受限环境下仍需提权绕过 `vite` 的 `spawn EPERM` 子进程限制，但当前未发现公开社区分发页新增编译回归。
