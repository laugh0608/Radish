# 2026 年 9 月开发日志

## 2026-09-05

- 完成 [Flutter Native P8-A desktop platform readiness](/records/f4-flutter-native-p8a-desktop-platform-readiness-2026-09-05)：确认仓库仅有 Android / iOS runner，Windows / macOS / Linux 当前统一落入 `unsupported` 内存 session / auth / follow-up，不能直接作为桌面产品；expanded 页面族与基础快捷键可继承，但桌面完整输入 / 窗口门禁仍缺。macOS 本机 Flutter `3.44.0`、Xcode `26.6`、CocoaPods `1.16.2` 与 desktop device 就绪；Windows11 ARM64 日常 VM 有 Git / Windows SDK，但缺 Flutter、CMake / Ninja / clang-cl 和 Visual Studio C++ Desktop workload，CleanBase 未启动；Debian 两 VM 待实时审计。P8 固定按 macOS、Windows、Linux、成组门禁推进，下一顺位为 P8-B macOS local platform foundation readiness。

## 2026-09-01

- 项目所有者确认 [P7-D Internal TestFlight-only 范围裁决](/records/f4-flutter-native-p7d-internal-testflight-owner-scope-2026-09-01)：近期 iOS 目标只到 Internal TestFlight，不开放 external tester / public link，不提交 App Review 或 App Store。账号删除、完整 Flutter UGC 举报 / 屏蔽、StoreKit / IAP 与商店 metadata 后置 D4；近期仍需关闭 testing Gateway fail-closed、Apple Developer / App Store Connect 基础、archive preflight、privacy manifest、export compliance、唯一 build、Internal Only group 与真机回归。P7-D 调整为 D1 minimal readiness、D2 development-signed 真机、D3 TestFlight Internal Only，D4 无限期后置；本裁决未授权 Apple 账号、签名、真机、archive 或上传操作。
- 完成 [Flutter Native P7-D iOS 真机、签名与分发 readiness](/records/f4-flutter-native-p7d-ios-device-signing-distribution-readiness-2026-09-01)：确认 `com.radish.client`、`26.8.2+1`、iOS `13.0+`、iPhone / iPad、`radish` OIDC scheme、Keychain entitlements、AppIcon、Release configuration、插件 privacy manifest 与本机 Xcode `26.6` 具备继续产品化基础，仓库没有 Apple Team、证书、provisioning、archive / IPA 或上传配置。实际真机 / 分发仍为 `No-Go`：testing / production build 可静默回落 localhost / 示例域名，Flutter 缺少账号删除、UGC 举报 / 用户屏蔽入口，站内胡萝卜余额可购买并解锁数字权益但没有 StoreKit / iOS channel 裁决，privacy / support / export compliance 与 App Store metadata 也未形成。P7-D 固定拆为 D1 合规能力门禁、D2 development-signed 真机、D3 signed archive + Internal TestFlight、D4 External TestFlight / App Store；下一顺位先完成 D1 方案，不读取签名材料或产生 Apple 外部状态。
