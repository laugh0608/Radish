# 当前进行中

> 本页是新会话快速入口，只维护 **当前阶段、最近进度、当前目标、下一顺位与维护线**。
>
> 历史批次、验证细节和长背景不要继续堆到本页；需要回顾时查看 [开发日志](/changelog/)、[已完成摘要](/planning/archive) 与对应专题文档。

## 当前状态

- **阶段**：`第三开发阶段：真实使用增长与长期契约治理`
- **当前主线**：`P3-8-D 多端主路径缺口补齐与验收矩阵`
- **复核日期**：`2026-06-04`
- **最近结论**：
  - `P3-1` 至 `P3-5` 已完成公开内容增长、PublicId 试点、留存回流、动态 sitemap 与详情 head snapshot 首批闭环
  - `P3-6` 公开增长部署观察已收口，本地 Gateway 与生产公开域名 `https://radishx.com` 的 public head smoke 均通过，转入维护线
  - `P3-7-A / P3-7-B` 已完成 WebOS / PC 工作台复访小闭环和高信号候选筛查，当前未发现新的 `P0/P1`
  - 原 `P3-7` 收尾口径偏向维护观察，无法给出最近阶段的明确开发入口；当前需要重新评估最近可推进任务，而不是直接切到远期专题
  - `P3-7-C` 已从原规划、backlog、未完成功能和近期主路径中选出下一批一天级任务：先做 WikiApp 热区治理，再做 ChatApp 热区治理
  - `P3-7-C1` 已完成 WikiApp 文档工作台首批热区拆分：侧边栏抽出为独立组件，纯 helper 迁出主文件，`WikiApp.tsx` 已降至硬上限以内
  - `P3-7-C2` 已完成 ChatApp 聊天工作台首批热区拆分：消息列表、频道侧栏、成员面板、输入区状态和纯 helper 已从主文件抽出，`ChatApp.tsx` 已降至硬上限以内
  - `P3-7-C3` 已完成后端 Service 首批候选治理：`ContentModerationService` 采用行为等价 partial 分文件拆出举报目标快照解析与队列导航装配，主文件从 `1735` 行降至 `806` 行
  - `ExperienceService` 已完成每日统计、经验治理观察规则、治理动作留痕、等级配置 / 缓存、交易记录、冻结状态、排行榜与管理员调整逻辑拆分，`ExperienceService.cs` 从 `2807` 行降至 `888` 行
  - `P3-7-C3` 作为当前主线可以阶段收口；剩余经验发放主流程不在本批硬拆，后续如需治理应单独评审
  - `P3-8-A` 已从后续重点切为当前主线并完成首批审计，约束仍是先做功能缺口与 UI 设计入口审计，不直接大面积实现
  - `P3-8-A` 首批审计矩阵已落到专题文档，并选出 `Flutter 公开榜单只读入口` 与 `Console 治理工作台设计端点` 作为首批推进项
  - `P3-8-B1` 已完成 Flutter 公开经验榜只读入口：新增原生榜单 tab、发现页跳转、公开经验榜 API 仓储、加载 / 空态 / 错误 / 刷新状态，并补齐 Flutter 单测与壳层 smoke
  - `P3-8-B2` 已完成 Console 治理工作台设计端点：`Docs/frontend/console-governance-workbench-design.md` 已定义信息架构、实现边界、验证入口和 `.pen` 源文件落点
  - `P3-8-C1` 已完成 Console 治理工作台结构基座首批实现：`ModerationPage` 与 `ExperienceAdminPage` 已完成 helper、列定义、局部组件拆分和行为等价布局承载，主文件分别降至 `843` / `712` 行
  - Console 设计稿已扩展为 `P01-P08` 编号页面，覆盖壳层基座、内容审核、经验台账、治理调度总览、表格 CRUD、设置策略与两个移动端治理视图；`Console 样式与 Token 使用说明` 已同步 Case Desk 风格、页面类型和“不硬套模板”规则
  - `P3-8-C2` 已完成 Console 设置型页面首个实现试点：`Settings` 按 `P06` 方向迁入分组导航、居中配置列和右侧影响范围摘要，并在 `adminFeature.css` 沉淀可复用设置型布局类
  - `P3-8-C2` 已完成 Console 表格 CRUD 页面试点：`UserList` 按 `P05` 方向迁入指标、筛选工具条、表格主体和右侧摘要栏，`AdminLayout` / `Breadcrumb` / `adminFeature.css` 继续收敛到 Case Desk 壳层与表格型布局基座
  - `P3-8-C2` 已完成 Console 调度总览页试点：`Dashboard` 按 `P04` 方向迁入总览指标、快捷操作、最近订单和右侧治理 / 运营调度入口，保留现有统计与订单 API
  - `P3-8-C2` 已继续完成同类历史列表页小步迁移：`TagList` 复用 `P05` 表格型布局基座，迁入指标、筛选工具条、表格主体和右侧摘要栏，保留标签 CRUD、启停、恢复、排序和分页行为
  - `P3-8-C2` 已继续完成 `CategoryList` 小步迁移：复用 `P05` 表格型布局基座，迁入指标、筛选工具条、表格主体和右侧摘要栏，保留分类 CRUD、启停、恢复、排序、层级显示和分页行为
  - `P3-8-C2` 已继续完成 `SystemConfigList` 小步迁移：复用 `P05` 表格型布局基座，迁入配置指标、筛选工具条、品牌图标配置面板、表格主体和右侧摘要栏，保留配置 CRUD、站点图标上传 / 恢复默认和筛选行为
  - `P3-8-C2` 已继续完成 `RoleList` 小步迁移：复用 `P05` 表格型布局基座，迁入角色指标、列表状态工具条、表格主体和右侧权限摘要栏，保留角色 CRUD、启停、权限配置跳转和分页行为
  - `P3-8-C2` 已继续完成 `Applications / StickerGroupList / StickerList` 小步迁移：复用 `P05` 表格型布局基座，迁入应用 / 表情包 / 分组表情指标、列表工具条、表格主体和右侧摘要栏，保留客户端 CRUD、密钥重置、表情包启停、表情排序与批量上传行为
  - `P3-8-C2` 已继续完成 `ProductList / OrderList` 小步迁移：复用 `P05` 表格型布局基座，迁入商城商品 / 订单指标、筛选工具条、表格主体和右侧摘要栏，保留商品 CRUD、上下架、关联订单跳转、订单详情、失败重试和管理员备注行为
  - `P3-8-C2` 已继续完成 `CoinAdminPage` 小步迁移：复用工具型布局基座，迁入余额指标、查询工具条、调账主区和右侧摘要栏，保留用户余额查询、管理员调账、权限判断和表单字段行为
  - `P3-8-C2` 已继续完成 `RolePermissionPage` 小步迁移：复用权限配置型布局基座，迁入页面标题卡、授权指标、角色信息卡、资源树主区和权限预览区，保留资源树勾选、父级继承、接口预览、并发保存参数和权限判断行为
  - `P3-8-C2` 已继续完成 `UserDetail` 小步迁移：复用详情型布局基座，迁入标题卡、用户指标、基本信息、最近胡萝卜流水 / 订单记录和右侧摘要栏，保留用户详情、资产、经验、订单查询与订单治理详情跳转行为
  - `P3-8-C2` 已继续完成 `UserProfile` 小步迁移：复用 `P06` 设置型布局基座，迁入个人资料标题卡、资料指标、头像摘要、基础资料表单和账号摘要栏，保留头像上传、个人资料保存、表单校验和 `UserContext` 刷新行为
  - `P3-8-C2` 首批高频 Console 页面类型试点可以阶段收口；继续默认筛剩余 CSS / 历史页面会进入低收益打磨，后续只在发现明显视觉断裂、权限页面问题或真实使用阻塞时回拉
  - `P3-8-D` 切为当前主线：先从移动 Web 公开视图验收矩阵起步，再评估 Flutter 下一批高价值功能或纯 Web 主路径补强
  - 多端路线已进一步收敛为 `纯 Web + Flutter` 主线：根路径 `/` 与默认浏览器入口转向纯 Web，`/desktop` 仅保留为 WebOS 历史入口，后续新功能默认不进入 WebOS；PC/Tauri 放到最后再评估，若重启也只做纯 Web 增强体验
  - `P3-8-D` 移动 Web 公开视图验收矩阵已建立，并完成 `390 x 844` Vite 直连实测；暂未发现新的 `P0/P1`，公开页顶部工作台入口已从 `/` 修正为 `/desktop`
  - 普通浏览器根路径 `/` 已切向纯 Web 公开分发页 `/discover`，Vite 直连与 Gateway 入口均已复核；Tauri 当前仍保留 `/desktop` 入口；历史 `?demo` 认证测试页已移除，`/oidc/callback` 保留为独立正式回调入口
  - `P3-8-D` 第二个纯 Web 小闭环已推进：公开个人页 `/u/:id` 补显式复制链接入口，forum / docs / shop / profile 详情类公开分享状态收敛到统一 hook，并修正底层剪贴板在嵌入浏览器 / 权限受限上下文下的同步 fallback
  - `P3-8-D` 第三个纯 Web 小闭环已推进：公开 forum / docs / profile / shop 详情来源返回状态写入 `history.state`，刷新或浏览器历史恢复后仍可保留来源返回语义，且不污染公开分享 URL
  - `P3-8-D` 公开商城购买回流小闭环已推进：商品详情只读购买提示的工作台入口从 `/` 修正到 `/desktop`，避免根路径切到 `/discover` 后购买 / 订单 / 背包回流被带回公开发现页
  - `P3-8-D` 公开商城到工作台上下文桥接已推进：公开商品详情的工作台入口改为 `/desktop?app=shop&productId=...`，桌面壳层支持解析 `shop` 商品深链并打开对应 Shop 商品详情
  - `P3-8-D` 工作台商城订单 / 背包入口承接已推进：桌面壳层支持 `shop` 的 `orderId`、`view=orders` 与 `view=inventory` 深链，订单和背包入口保持登录后消费，避免未登录时误显空订单或空背包
  - `P3-8-D` 纯 Web 登录后轻量链路闭环已推进：未登录用户从公开商品页进入工作台商品详情后，可保存商品上下文登录，并在 OIDC 回调后恢复到原商品详情继续购买
  - `P3-8-D` Flutter 榜单到公开主页小闭环已推进：经验榜条目可打开原生公开主页，并通过 Android Back 回到榜单
  - `P3-8-D` Flutter 通知回流小闭环已推进：最新论坛通知打开原生帖子详情时保留打开前 tab，详情返回后回到原位置
  - `P3-8-D` Flutter 轻回应登录回流小闭环已推进：从帖子详情轻回应区发起登录后，可回到当前帖子轻回应区并继续发布
  - `P3-8-D` Flutter 公开个人页来源返回批量验收已推进：发现、论坛作者和榜单进入公开主页后，Android Back 可回到原来源；公开主页内容详情返回后仍保留来源 tab，并已补直接 Android Back 从帖子 / 评论详情退回公开主页再回榜单的 shell smoke
  - `P3-8-D` 纯 Web / 工作台论坛登录回流小闭环已推进：桌面入口支持 `forum` 深链，论坛发帖、评论和轻回应登录入口统一走 `redirectToLogin({ returnPath })`，登录后可回到论坛工作台或原帖子 / 评论上下文
  - `P3-8-D` Dock 主动登录回流小闭环已推进：工作台右侧登录按钮会保存当前合法 `/desktop...` 路径，避免用户在 forum / shop 等保留入口主动登录后落回 `/discover`
  - `P3-8-D` 公开商城工作台入口契约已收口：公开商品详情复用统一 `buildDesktopShopProductReturnPath` 构造工作台入口，非法商品详情路径明确回落商城首页
  - `P3-8-D` 公开商品榜单到商品详情小闭环已推进：公开热门商品榜单条目可进入公开商品详情，并通过来源状态返回榜单，不直接打开订单或背包流程
  - `P3-8-D` Gateway 公开页资源 URL 收口已推进：HTTPS Gateway 下的本地 HTTP 媒体 / favicon / Markdown 附件资源地址会归一到当前 Gateway origin，减少 Schemeful Same-Site Cookie 告警
  - `P3-8-D` 移动 Web 公开阅读链路小闭环已推进：共享 Markdown、公开 docs 详情和公开 forum 详情补齐窄屏长链接、代码块、表格、图片、标题、slug 和返回 / 分享按钮的防横向溢出约束；公开分享链接统一走运行时公开域名配置并保留 docs 锚点
  - `P3-8-D` 移动 Web 公开阅读链路二轮复核已推进：公开 docs Markdown 相对文档链接 / 同页锚点统一走公开壳层路由，阅读容器继续补齐嵌套 flex / 卡片场景的窄屏防撑开约束
  - `P3-8-D` 公开来源返回批量验收已推进：公开详情页的显式来源返回会保留既有 `history.state` 来源链路，避免 `discover -> forum detail -> profile -> 返回 forum detail` 后形成来源循环
  - `P3-8-D` 移动 Web 公开分发页二轮复核已推进：`/discover` 论坛卡片进入公开帖子详情时优先使用 `Post.PublicId`，与 forum 列表 / 搜索 / 标签页的公开 URL 口径保持一致
  - `P3-8-D` 移动 Web 公开视图矩阵已阶段收口：批量复核 `/discover / forum / docs / shop / leaderboard / u/:id` 的窄屏布局、来源返回、分享入口、公开只读边界、长文本防溢出和公开链接契约；本轮未发现新的 `P0/P1`，后续不再逐页小循环式打磨 Web 公开页
  - `P3-8-D` Flutter 公开商品只读详情小闭环已推进：发现页商城精选商品可打开原生只读详情，并通过 Android Back 回到发现页；本轮不扩展完整移动商城、购买、订单、背包、支付口令或权益激活
  - `P3-8-D` Flutter 轻量 forum 通知列表已推进：已登录壳层可展示最近少量可跳 forum 通知，选择后复用帖子 / 评论 handoff 并保留打开前 tab；本轮不扩展完整通知中心、已读 / 删除、系统推送、完整移动商城或创作器
  - `P3-8-D` Flutter 公开商城列表只读入口已推进：发现页可进入公开商城列表，列表项可打开只读商品详情并返回商城列表；本轮不扩展购买、订单、背包、支付口令或权益激活
  - `P3-8-D` Flutter 论坛详情评论发布与回复已推进：已登录用户可在原生帖子详情发布根评论、回复根评论或子评论，提交后局部更新评论区并保留评论上下文；本轮仍不扩展发帖编辑器、点赞、投票、审核治理或富文本评论
  - `P3-8-D` Flutter 论坛评论登录回流与来源回归已推进：匿名态从评论区登录后回到评论输入区；公开主页评论上下文进入详情并回复后，Android Back 仍返回原公开主页来源
  - `P3-8-D` Flutter 原生公开详情分享入口已推进：forum detail、docs detail 与 shop detail 均可展示并复制完整公开链接，复制口径统一为 Gateway Base URL 加 Web 公开路由；本轮不扩展系统分享 SDK
  - `P3-8-D` Flutter 原生公开文档阅读链路已推进：只读 Markdown 阅读器可识别 `/docs/:slug`、完整公开 URL、`docs/:slug`、`./:slug` 与普通相对 slug 文档内链并打开原生 docs detail；页内锚点、附件路径和非 docs 链接继续按文本展示
  - `P3-8-D` Flutter 原生公开主页链接 / 展示 / 来源返回复核已推进：公开主页可展示并复制对应 Web 公开链接，匿名 / 已登录公开主页边界和长文本约束沿用既有 profile 测试覆盖，榜单进入公开主页再进入帖子 / 评论详情的 Android Back 来源返回 smoke 已保持通过
  - `P3-8-D` 纯 Web forum 公开详情到工作台参与回流已推进：公开帖子详情新增进入工作台轻回应 / 讨论入口，`/desktop?app=forum&postPublicId=...&intent=...` 可在登录前后保留帖子和操作区域上下文
  - `P3-8-D` 纯 Web shop 登录态购买意图回流已推进：公开商品详情和工作台商品详情的购买入口会携带 `intent=purchase`，匿名用户登录后回到同一商品并自动打开购买流程；订单 / 背包私有深链继续由 `/desktop` 门禁承接
  - `P3-8-D` Flutter 登录态商城订单 / 背包只读入口已推进：登录态“我的”页可打开原生订单列表与背包只读页，复用现有 `Shop/GetMyOrders`、`Shop/GetMyBenefits`、`Shop/GetMyInventory` 私有接口，覆盖加载 / 空态 / 错误 / 刷新 / 返回状态；本轮不扩展购买、取消订单、支付口令、权益激活、道具使用、完整移动商城、完整通知中心或 WebOS 新功能
  - `P3-8-D` Flutter 订单详情与背包来源只读承接已推进：订单列表可打开原生订单详情并返回，背包权益 / 道具可查看来源订单或来源商品并返回背包；本轮仍保持只读，不扩展购买、取消订单、支付口令、权益激活、道具使用、完整移动商城、完整通知中心或 WebOS 新功能
  - `P3-8-D` Flutter 登录态通知只读列表已推进：已登录壳层读取完整站内通知列表，展示标题、内容、类型、已读状态和时间；forum 通知仍可回到帖子 / 评论，系统等不可跳通知保持只读展示；本轮不扩展标记已读、删除、推送、完整通知中心、完整移动商城或 WebOS 新功能
  - `P3-8-D` Flutter 登录态胡萝卜资产只读入口已推进：登录态“我的”页可打开原生胡萝卜资产页，复用 `Coin/GetBalance` 与 `Coin/GetTransactions` 私有接口，覆盖余额、冻结余额、累计统计、最近流水、加载 / 空态 / 错误 / 刷新 / 返回状态；本轮不扩展转账、打赏、调账、支付、完整资产中心或 WebOS 新功能
  - `P3-8-D` Flutter 登录态经验记录只读入口已推进：登录态“我的”页可打开原生经验记录页，复用 `Experience/GetMyExperience` 与 `Experience/GetTransactions` 私有接口，覆盖等级、经验进度、冻结状态、最近经验流水、加载 / 空态 / 错误 / 刷新 / 返回状态；本轮不扩展经验调整、冻结治理、管理员复核、完整资产中心或 WebOS 新功能
  - `P3-8-D` Flutter 登录态“我的浏览记录 / 最近访问”只读入口已推进：登录态“我的”页可打开原生最近访问页，复用 `User/GetMyBrowseHistory` 私有接口，覆盖公开帖子 / 文档 / 商品详情承接、加载 / 空态 / 错误 / 刷新 / 加载更多 / 返回状态；本轮不扩展清空 / 删除、推荐系统、完整浏览历史治理、完整移动商城或 WebOS 新功能
  - `P3-8-D` Flutter 通知已读状态受控写入已推进：Flutter API 客户端补齐 `PUT` 契约，通知仓储复用 `Notification/MarkAsRead`，已登录通知列表支持单条显式标记已读并局部更新状态；本轮不扩展批量已读、删除、通知设置、系统推送或完整通知中心
  - `P3-8-D` Flutter 纯文本发帖受控写入已推进：论坛页读取顶级分类，登录态用户可发布纯文本帖子，成功后刷新列表并打开新帖子详情；本轮不扩展富文本、附件、投票、抽奖、草稿箱、编辑或点赞
  - `P3-8-D` Flutter 单商品购买路径受控写入已推进：登录态用户可从原生商品详情检查购买资格、输入支付口令购买 1 件商品，并在成功后进入订单详情确认结果；匿名态购买入口会发起登录并回到当前商品详情；本轮不扩展购物车、退款、权益使用、通知中心、完整移动商城或 WebOS 新功能
  - `P3-8-D` 胡萝卜资产发放与调账口径已回拉治理：注册、登录补偿和首个管理员初始化统一走注册奖励契约，Console 调账和按用户查余额会校验真实用户，避免给不存在的 UserId 建立余额并误导购买链路
  - Console 用户 ID 精度问题已按 `ID Phase A` 回拉：当前登录用户、个人资料和用户列表中的 `VoUserId / uuid` 保持字符串传递，避免 JavaScript 大整数精度丢失导致 Console 展示或调账指向错误用户；后续应补自动检查防止 `Number(...Id)` 回潮
  - `radish.client` 商城工作台构建 chunk warning 已完成治理：`ShopApp` 页级懒加载和商城分包规则已把 `app-shop` 收敛到 500k 警告阈值以内；仓库不保留 npm update notifier 配置
  - `2026-06-02` Flutter 单商品购买真实数据人工复核已通过，随后 Web 工作台购买回流补资格检查，避免资格不通过时仍打开支付口令弹窗
  - `ID Phase A` 第二轮已推进：`radish.client` 登录会话内部 `userId / tenantId` 迁为字符串口径，`npm run check:long-id-safety` 已接入 `validate:identity`，Flutter LongId 扫描已覆盖 map 读取、`int.tryParse`、`_readInt` 和 `as int` 等回潮形态
  - `ID Phase A` 第三轮已推进：`radish.client`、`radish.console` 与 `@radish/ui` 的高信号外部 ID 类型继续收敛为字符串，`check-long-id-safety` 已补 `string | number` 联合类型回潮扫描
  - `ID Phase A` 第三轮小写参数审计已收口：附件 / 分片上传、内容治理、排行榜、论坛分类、公开路由、工作台回跳和聊天 / 论坛导航中的外部 ID 契约继续收敛为字符串，`check-long-id-safety` 已覆盖小写 ID 声明回潮
  - Console 订单 / 胡萝卜流水排障链路已补强：订单详情可展示扣款流水 ID，并按 `BusinessType=Order / BusinessId=OrderId` 跳转定位对应胡萝卜流水；管理端流水查询支持业务上下文筛选
  - Flutter 订单 / 胡萝卜流水排障链路已补强：原生订单详情可在存在扣款流水 ID 时进入筛选后的资产流水页，用户侧 `Coin/GetTransactions` 支持按 `businessType / businessId` 查询；本轮仍不扩展退款、权益使用、转账、调账或完整资产中心
  - 购买 / 资产链路跨端排障复核已收口：Flutter 订单、Console 订单、胡萝卜流水和用户详情四个入口已能围绕同一笔购买按订单 ID、业务类型、业务 ID、扣款流水和用户上下文互相定位；后续不继续扩展完整移动商城、完整资产中心或完整财务后台
  - Console 角色授权链路已按 `ID Phase A` 回拉：角色 ID、资源 ID 和 API 模块 ID 在角色列表、角色编辑、授权快照、资源树勾选、权限预览和保存请求中保持字符串契约，`check-long-id-safety` 已补角色 / 资源 / API 模块 ID 回潮扫描
  - Flutter 登录态个人资料编辑已推进：登录态“我的”页可编辑用户名、邮箱、展示名称、年龄和地址，复用 `User/GetMyProfile` 与 `User/UpdateMyProfile` 私有契约，保存成功后刷新原生公开资料摘要；本轮不扩展头像上传、完整账号设置或关注管理
  - Flutter 个人资料写入后的跨端展示一致性已完成治理与人工复核：论坛帖子 / 评论 / 轻回应作者展示名回到当前用户资料口径，Console 用户详情补展示名称，同一用户在 Flutter、公开主页、Console 与论坛作者展示中未发现新问题
  - `ID Phase A` 自动化守护复扫已通过：`validate:identity` 未发现身份语义、协议输出或外部 LongId 字符串安全回归
  - Flutter 纯文本发帖后的公开详情链路已完成复核：发帖成功后打开新帖详情，详情接口返回的 `VoPublicId` 可展示为 `/forum/post/:publicId` 公开链接，未回退到内部帖子 ID；本轮不扩展富文本、附件、投票、抽奖、草稿箱、编辑或点赞
  - Flutter 纯文本发帖失败边界已补定向守护：已登录用户提交失败时保留草稿、展示服务端错误、不误打开帖子详情或成功状态
  - 项目仍处于单人开发期和功能建设期，没有稳定用户反馈和专职测试，不能把“等待真实使用观察”作为默认主线

## 当前执行入口

- [开发路线图总览](/development-plan)
- [第三开发阶段：真实使用增长与长期契约治理](/planning/phase-three-real-usage-contract-governance)
- [第二阶段产品功能补全规划](/planning/phase-two-product-completion)
- [前端多壳层策略](/frontend/shell-strategy)
- [多端客户端路线评估方案](/planning/multiplatform-client-route-evaluation)
- [P3-8 多端功能补全与 UI 设计治理](/planning/p3-8-multiplatform-feature-ui-governance)
- [Console 治理工作台设计端点](/frontend/console-governance-workbench-design)
- [验证基线说明](/guide/validation-baseline)

## 当前目标

1. **推进 P3-8-D 多端主路径缺口补齐与受控写入评估**
   - 移动 Web 公开视图验收矩阵已阶段收口，除维护线阻断外不再逐页打磨 Web 公开页
   - Flutter 已连续补齐商品、通知、商城列表、论坛评论、公开详情分享、docs 阅读链路、公开主页链接复核、登录态商城订单 / 背包只读入口、订单详情与背包来源承接、登录态通知只读列表、登录态胡萝卜资产、经验记录、最近访问只读入口、通知已读状态、纯文本发帖和单商品购买；纯 Web 已回拉 forum 公开详情到工作台参与回流和 shop 登录态购买意图回流；后续继续从真实移动主路径或跨端回流缺口中选择下一项可验证任务
   - Flutter 不再把“只能只读”作为长期总门禁；已经在 Web / API 成熟、能按单一用户动作验证的能力，可进入受控写入评估
   - WebOS 只维护 `/desktop` 保留入口和迁移过渡，不作为新增功能目标
   - 只回拉能影响真实访问、移动阅读、跨端回流、购买 / 订单 / 背包、权限授权或治理效率的高信号问题
2. **维护观察降级为并行线**
   - `P3-6 / P3-7` 暴露的新问题仍可回拉小闭环，但不再占用主线
   - 维护线只处理公开访问、head / sitemap、分享预览、运行日志、购买 / 订单 / 背包、权限授权等高信号问题
3. **保留关键门禁**
   - 大页面重设计、端点级视觉治理、跨页面视觉体系变更原则上先更新 Pencil 设计稿
   - 纯功能缺口、后端治理、行为等价拆分、小范围表单 / 状态 / 文案修正不被 Pencil 前置阻塞
   - Flutter 受控写入每次只允许一个真实用户动作，并必须覆盖登录回流、错误态、返回状态和定向测试
   - 不把完整 E2E、完整可观测性平台或大而全运营平台作为继续开发前置

## 下一顺位

- `P3-8-D 多端主路径缺口补齐与验收矩阵`
  - 第一顺位：移动 Web 公开视图验收矩阵已阶段收口；除公开访问、分享预览、购买 / 订单 / 背包、权限授权等维护线阻断外，不再继续逐页小循环式打磨 Web 公开页
  - 第二顺位：Flutter 登录态“我的浏览记录 / 最近访问”只读入口已完成，移动端复访主路径阶段收口
  - 第三顺位：通知已读状态、纯文本发帖与单商品购买受控写入已完成，Flutter 单商品购买真实数据人工复核已通过；下一批不继续扩展完整移动商城，优先回到跨端回流、公开访问、购买 / 订单 / 背包、权限授权或 ID 守护中的高信号缺口
  - 第四顺位：纯 Web 登录后轻量链路已补 forum 参与和 shop 购买意图回流；后续只在跨端回流、公开访问或购买 / 订单 / 背包出现高信号缺口时回拉
  - 第五顺位：`ID Phase A` 首批自动化守护已落地；后续继续按 [ID 与联邦路线图](/architecture/id-and-federation-roadmap) 只做外部 LongId 字符串安全和内部会话口径收敛，不启动完整 `PublicId` 全量迁移或数据库主键迁移
  - WebOS / PC 工作台转保留迁移线：只处理 `/desktop` 保留入口的阻断级问题和迁移所需缺口，不再作为新功能承载方向；PC/Tauri 放到最后再评估，且不绑定 WebOS
  - Console C2 转维护：只在真实使用、权限页面或明显视觉断裂暴露问题时回拉；保持 API、权限、表单字段、数据契约和治理动作语义不变
- `P3-7-C3` 进入维护 / 后续评审池
  - `ExperienceService` 剩余经验发放主流程如需继续拆分，应单独评估事务、重试、升级奖励和冻结语义风险
  - 不把继续拆热区作为当前主线的默认下一步

## 明日事项

- 第一顺位：基于 `P3-8-D` 已收口矩阵选择下一项高信号任务，优先单一真实用户动作或单一治理排障动作，而不是扩展完整移动商城、完整资产中心、完整账号设置或完整财务后台
- 第二顺位：继续维护购买 / 订单 / 背包、权限授权、公开访问和 `ID Phase A` 自动化守护；只在真实阻断、排障定位缺口、权限页面问题、`check-long-id-safety` 或真实编译暴露新命中时回拉，不启动完整 `PublicId` 全量迁移或数据库主键迁移
- 第三顺位：若下一轮没有暴露高信号缺口，优先从 Flutter / 纯 Web 已成熟接口中选择下一个真实用户动作或治理排障动作；`ID Phase A` 仅在扫描命中或新增外部 ID 边界时回拉
- 继续遵守受控写入边界：每次只推进一个真实用户动作，不扩展完整通知中心、完整移动商城、完整创作器、富文本、推送、权益使用、退款或 WebOS 新功能
- 本轮不启动完整 `PublicId` 全量迁移、数据库主键迁移、联邦预研或大范围 UI 重构

## 并行维护项

- 公开 head smoke、动态 sitemap、head snapshot 缓存与 `GatewayService:PublicUrl` / `RADISH_PUBLIC_URL` 生产域名配置维护
- `M14` 宿主运行与最小可观测性基线、`M15` 最小交付与部署基线
- `validate:baseline / validate:baseline:host / validate:ci / Identity Guard`
- 桌面壳层、窗口几何记忆、主题切换、聊天室、通知中心、商城等既有能力稳定维护
- Console 新增或改动页面继续优先复用 `@radish/ui` 组件、交互反馈和主题 token

## 当前不做

- 把维护观察继续当作唯一主线
- 等真实用户反馈出现后才继续开发未完成功能
- 跳过 `P3-8-A` 审计直接启动整站 UI 重构或多端同时重写
- 绕过功能缺口矩阵和端点分组，直接把所有页面改成同一套布局
- 继续把 Console 剩余 CSS / 历史页面筛查当作默认主线
- 直接拍死所有 `.pen` 文件拆分；端点粒度需在后续专题中结合实际页面和客户端边界决定
- 绕过 Pencil / 设计稿直接大面积改 UI；但小范围功能、状态、表单、文案或行为等价修正不被 Pencil 前置阻塞
- 把 Tauri 当作原生 UI 重写路线或 WebOS 默认分发路线
- “移动版 WebOS”
- 新功能默认进入 WebOS
- PC 客户端抢占纯 Web / Flutter 主线
- 继续把 Flutter “只能只读”作为长期总门禁
- 一次性启动完整移动商城、完整通知中心、完整资产中心、完整创作器或完整浏览历史治理
- 完整 `PublicId` 全量迁移或数据库主键迁移
- 完整 Playwright / E2E 平台
- 完整可观测性平台或大而全运维平台

## 文档更新规则

- 本页必须保持简短，面向新会话快速读取
- 没有主线切换、优先级变化或新的关键事实，不改本页
- 功能开发细节、命令级验证记录、批次流水和历史背景不写入本页
- 后续多端功能与 UI 治理细节统一写入 [P3-8 多端功能补全与 UI 设计治理](/planning/p3-8-multiplatform-feature-ui-governance)
