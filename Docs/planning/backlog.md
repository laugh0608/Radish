# 未来规划 / Backlog

> 本页维护 **当前未启动，但已进入规划视野的任务**。
>
> 已启动事项请看 [当前进行中](/planning/current)，已完成事项请看 [已完成摘要](/planning/archive)。

## 当前后置原则

- 首版 `dev` 范围以 [首版 dev 边界](/planning/dev-first-scope) 为准；未被纳入首版边界的事项，默认后置。
- 自 `2026-03-18` 起，社区非阻塞增强项不再作为当前主线，而是统一后置到首个 dev 版本之后再评估。
- 当前 backlog 里的社区增强，默认都不是“现在就该做”，除非后续被确认会阻塞首个 dev 版本。
- 首个 dev 版本之前，优先级更高的是全局主线补齐、国风主题 / `radish.client` i18n 首轮落地，以及验证基线与首轮交付链。

## 首个 dev 版本后再评估

### 1. 论坛投票扩展（P3-ext）

**目标**
- 在已完成的投票 MVP 基础上，继续增强更完整的回归、筛选与交互体验
- 保持“列表轻摘要、详情重交互”的当前论坛设计边界
- 不在当前阶段扩张到独立投票应用、复杂活动页或后台投票治理

**进入条件**
- 投票 MVP 已稳定运行，团队确认继续深挖论坛内容互动链路
- 首个 dev 版本已具备可发布条件，团队确认可以回头补社区互动增强
- 不希望当前阶段过早引入复杂活动运营边界

**参考方案**
- [论坛投票 MVP 设计方案](/features/forum-poll-mvp)

**当前说明**

- 该项已从当前主线移出，默认后置到首个 dev 版本之后再评估

### 2. 论坛问答扩展（P4-ext）

**目标**
- 在已完成的问答 MVP 基础上，继续增强“答案视图 + 讨论协同”的体验
- 保持“列表轻摘要、详情重交互”的当前论坛设计边界
- 不拆独立问答 App，不并行引入悬赏、奖励与复杂排序

**进入条件**
- 论坛问答 MVP 已稳定运行，团队确认继续深挖社区内容链路
- 首个 dev 版本已具备可发布条件，且团队确认问答方向优先级高于其他增强项
- 不希望当前阶段过早引入更重的活动运营或治理边界

**参考方案**
- [论坛问答 MVP 设计方案](/features/forum-qa-mvp)

**当前说明**

- 首轮最小范围已完成并收口：问答视图、问答状态筛选、问答专用排序、回答区排序切换、回答区轻筛选与问题历史入口已先行落地
- “问题历史”关键后端回归与手工回归已完成，当前不再把本项视作已启动主线
- 后续若继续推进，本项只保留更深的问答增强，不再重复补首轮基础能力，且统一后置到首个 dev 版本之后

### 3. 论坛抽奖扩展（P5-ext）

**目标**
- 在已完成的抽奖 MVP 基础上，补齐“活动能力”真正需要的后续扩展项
- 保持与论坛帖子主链的耦合关系，不急于拆独立抽奖应用
- 仅在团队明确接受更重的活动治理边界后再启动

**进入条件**
- `P5` MVP 已稳定运行，并完成一轮真实联调结论沉淀
- 首个 dev 版本已具备可发布条件，团队确认仍要继续深挖抽奖
- 可以接受自动开奖、奖励联动、防刷、公示等边界带来的复杂度上升

**当前说明**

- 当前最小闭环已完成：发帖附带抽奖、父评论参与、手动开奖、中奖通知、论坛通知跳转与浏览记录回看
- 现阶段不应把自动开奖、奖励联动、防刷公示等增强项提前拉回当前主线，统一后置到首个 dev 版本之后

### 4. Console 后续扩展（Console-ext Phase 2+）

**目标**
- 承接一期完成后的继续扩展项
- 在已落地授权面板基础上，再评估更完整的权限中心、审计与共享接口治理
- 保持与当前 `ApiModule + RoleModulePermission + VoPermissions` 链路兼容，不重开一套权限体系

**进入条件**
- 一期已稳定上线并通过回归
- 首个 dev 版本已具备可发布条件，团队确认需要继续往更完整的权限中心扩展
- 不将范围直接扩张为完整 IAM 平台

**当前说明**

- `Console-ext` 一期已完成本轮收口
- 当前不再作为 dev 首版前主线
- 一期专题文档见：[Console 权限 / 菜单 / 按钮管理一期设计方案](/guide/console-authorization-phase1)

### 5. 开源软件清单自动生成与发布物公告（P2-ext Auto）

**目标**
- 在已完成的欢迎页内容扩展版基础上，继续补齐更自动化的第三方依赖公告能力
- 视需要补齐完整许可证文本、全量依赖清单、构建时自动生成或发布物公告
- 保持与当前产品规模匹配，避免过早引入重型构建治理

**进入条件**
- 当前欢迎 App 静态清单已不能满足发布或合规需要
- 社区主链与权限主链路进入稳定维护
- 不与更大治理项形成资源冲突

**当前说明**

- `P2-ext` 的内容扩展版已完成，本项只保留更重的自动化 / 发布物方向，并后置到首个 dev 版本之后

### 6. Later：邮件通知系统

**定位**
- 统一归类为影响面更广、复杂度更高的大功能或大治理项
- 暂不作为当前阶段的近期待进入事项

**后置原因**
- 邮件通知系统涉及模板、触发、投递与运维边界，复杂度较高

**重新启动条件**
- 首个 dev 版本已具备可发布条件
- 当前首版范围内的主线与交付链不再频繁返工
- 团队可接受一次较大范围的横向治理改造

## 首个 dev 版本前优先路线

> 以下三项虽然仍在 backlog 视野内，但优先级高于社区体验增强，属于首个 dev 版本之前应优先推进的主线路线。

### M13：验证基线与回归资产工程化

**目标**
- 把当前已经存在但分散的验证资产收束成统一的工程基线
- 优先整合 `dotnet build/test`、前端 `type-check`、`radish.client` 现有 `node --test`、`HttpTest`、权限扫描脚本与身份语义扫描规则
- 先形成“可重复执行、与当前代码相符”的本地/仓库级校验入口，再决定是否接入 CI

**启动清单（当前已具备的真实资产）**
- 后端构建 / 测试：`Scripts/dotnet-local.ps1 build|test` 与常规 `dotnet build/test`
- 前端校验：根目录 `npm run type-check`、`npm run test --workspace=radish.client`
- 专题扫描：`npm run check:console-permissions`
- 联调资产：`Radish.Api.Tests/HttpTest/*.http` 与其目录 `README`
- 宿主只读自检：`Radish.DbMigrate doctor`、`Radish.DbMigrate verify`

**阶段边界**
- 当前先整合“已有资产”，不把阶段默认扩张为完整 CI、全量 E2E、Playwright 或统一流水线平台建设
- `HttpTest` 与最小人工验收继续保留，但要明确与构建 / 类型检查 / 脚本扫描的分层关系
- 身份语义防回归规则优先沉淀为仓库脚本入口，再决定是否上升到流水线

**预期输出**
- 后端、前端、专题脚本与扫描规则形成统一的验证清单
- 身份语义 Phase 5 防回归规则落地为仓库脚本或可复用校验入口
- 关键主链路的 `HttpTest` / 最小人工验收与构建校验有清晰分层，不再依赖口口相传
- 是否接入 CI 作为阶段尾声的可选决策，而不是默认前提

**当前落地（首轮）**
- 根目录统一入口已补齐：`npm run validate:baseline`
- 已区分 `quick / full / host` 三层验证方式
- `HttpTest` 仍保留为专题回归层，不并入默认自动化入口

### M14：宿主运行与最小可观测性基线

重定义入口见：[M14 宿主运行与最小可观测性基线（重定义）](/guide/m14-host-runtime-observability-baseline)

**目标**
- 以当前真实宿主为基础，先收束 API / Gateway / Auth 的最小运行、自检与排障基线
- 在已有 Serilog、HealthCheck、`DbMigrate doctor/verify` 与 `validate:baseline:host` 基础上，形成更统一的“看得见、查得到、能自检”的最小观测口径
- 不直接把阶段目标膨胀成完整 Tracing / Metrics 平台建设，也不把它重新泛化成“部署与运维”大阶段

**预期输出**
- 宿主配置一致性、只读自检、健康检查、启动日志与部署复核入口之间的关系更统一
- `Auth` / `Api` / `Gateway` 的最小运行观测边界更加一致
- 对“哪些问题已属于下一轮更重的可观测性平台建设”有明确边界说明

### M15：最小交付与部署基线

**目标**
- 把当前已经存在但尚未完全落地的部署资产整理成真正可执行的最小交付方案
- 明确哪些 Docker / Compose / 宿主镜像能力是仓库现实，哪些仍只是文档草案
- 形成“可按步骤验证”的发布与回滚基线，而不是一次性承诺完整平台化交付

**预期输出**
- `Radish.Api` 现有 Dockerfile、部署指南与宿主边界口径统一
- 是否补齐 `Gateway` / `Auth` 镜像与 Compose 编排，形成明确结论和进入条件
- 发版、部署、自检、回滚文档与仓库真实资产一致

**当前说明**
- 截至 `2026-04-06`，`M15` 已进入第一批执行，当前统一以 [当前进行中](/planning/current) 与 [M15 最小交付与部署基线](/guide/m15-delivery-baseline) 为准
- 本页暂只保留阶段目标摘要；第一批完成后，再决定是否继续扩展到更重的交付治理

## 暂缓专题

### Gateway & BFF 深化

**状态**
- 暂缓，不作为当前阶段目标

**重新启动条件**
- 多服务 / 多入口边界真正稳定
- 团队确认存在明确的聚合 API、认证透传或客户端编排需求，而不是仅凭“架构上看起来应该有”来启动

## 维护规则

- 只有“短期内可能进入执行”的事项才放进本页
- 首个 dev 版本之前，社区非阻塞增强默认只保留在本页，不上提为当前主线
- 已启动就迁移到 `planning/current.md`
- 已完成就只保留摘要，迁移到 `planning/archive.md` 或 `changelog/`
