# P3-12-E5-B 受控写入旅程验证记录

> 日期：2026-07-06（Asia/Shanghai）
>
> 范围：Gateway 真实页面受控写入复核
>
> 入口：`https://localhost:5000`

## 结论

本轮使用本地种子登录态 `Admin#2` 完成受控写入旅程验证，批次标记为 `E5B-20260706121844`。

论坛发帖 / 评论、Docs 作者保存 / 阅读回流、商城购买确认边界和 Console 订单备注均通过真实页面路径验证，当前未发现阻断 `P3-12-E6` 发布候选进入判断的 `P0/P1` 问题。

验证过程中命中一个商城库存文案一致性问题：同一无限库存商品列表展示为可购买，详情页展示为 `0 件商品`。本轮已将前端库存类型判断统一兼容 C# enum 数字序列化与字符串序列化，避免无限库存详情误读为 0 件库存。

## 复核方式

- 工具：Chrome 插件真实页面复核。
- 登录态：本地种子账号 `Admin#2`；Console 首次进入时在本地 OIDC 授权页确认 `radish-console` 授权。
- 业务写入边界：只执行可控测试内容、登录态文档草稿和 Console 低风险备注；商城只进入支付口令确认弹层，不提交支付口令，不执行最终扣费。
- 浏览器日志：Chrome `warn / error` 日志采集结果为 `0`。
- 工具限制：Chrome `domSnapshot()` 在当前页面报 `TypeError: incrementalAriaSnapshot is not a function`，本轮改用受限 DOM 读取、唯一控件计数和可见文本核对完成复核。

## 覆盖矩阵

| 旅程 | 受控动作 | 结果 |
| --- | --- | --- |
| 登录态确认 | 访问 `/me` | 识别当前账号 `Admin @Admin#2`，登录态有效。 |
| 论坛发布 | `/forum/compose` 填写标题、正文、分类 `技术交流`、标签 `技术交流` 并发布 | 生成帖子 `https://localhost:5000/forum/post/2074106100868710400`，详情页显示标题 `E5-B 受控写入旅程验证 E5B-20260706121844`。 |
| 论坛评论 | 在同一帖子详情发布根评论 | 评论 `E5-B 受控写入评论验证：E5B-20260706121844` 可见，讨论数从 0 增至 1。 |
| Docs 作者保存 | `/docs/compose` 新建登录可见草稿 | 保存后进入 `/docs/edit/2074106391756275712`，标题和摘要可见。 |
| Docs 阅读回流 | 访问 `/docs/e5b-e5b-20260706121844` 与 `/docs/mine` | 阅读页显示 `登录可看 / 草稿`、正文与批次标记；我的文档列表可回读同一标题。 |
| 商城购买边界 | `/shop/product/100062` 点击 `立即购买` | 打开购买确认弹层，显示数量、总价、支付口令输入和禁用态 `确认购买（50 胡萝卜）`；未提交支付口令，未扣费。 |
| Console 备注 | `/console/orders` 打开订单 `P3D32-ORDER-20260630-001` 详情并追加管理员备注 | toast 显示 `订单备注已保存`，摘要和详情中可见 `E5-B 受控写入旅程验证：E5B-20260706121844`。 |

## 代码修正

- `Frontend/radish.client/src/api/shop.ts`
  - 新增 `isUnlimitedStockType()`，兼容 `Unlimited`、大小写字符串和数字 `0`。
- `Frontend/radish.client/src/public/shop/PublicShopViews.tsx`
  - 公开商城详情库存文案改用统一判断。
- `Frontend/radish.client/src/apps/shop/pages/ProductDetail.tsx`
  - WebOS 商品详情库存文案改用统一判断。

## 验证

- `npm run build --workspace=radish.client`：通过。

## 限制与后续

- 本轮未执行注册提交、轻回应、问答回答、聊天发送、支付口令提交、最终购买扣费、退款 / 售后、Console 高风险治理动作、断网 / 弱网或慢接口注入。
- 受控测试帖子、评论、Docs 草稿和 Console 备注保留在本地开发数据中，用于后续追踪本批次复核。
- `P3-12-E5` 的高价值受控写入路径已经覆盖一轮，下一步可进入 `P3-12-E6 发布候选进入判断`；若 E6 复核命中新的接口、错误模型、审计、支付或权限边界缺口，再回拉对应专题补方案。
