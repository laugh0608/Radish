# F4 2026-08-03 日终提交回顾与文档审阅

> 日期：2026-08-03（Asia/Shanghai）
>
> 范围：按提交日期统计的 5 个当日提交；本次日终纯文档提交自身不计入回顾范围。

## 今日结论

- family-ui `v26.7.3` 破坏性基线升级有效：共享 CSS / JSON 副本使用灰玉参考默认并新增 `text-on-brand`，Client 项目层仍显式保留当前 `guofeng` 胭脂运行时值，品牌与操作前景语义没有被混并。
- 已建立唯一活动设计源 `radish-web-family-ui-v1.pen`，原四个大型 `.pen` 转为只读留档；普通迭代继续更新 `v1`，不创建并行活动文件。
- `R1-F01` 共享基础矩阵已完成；代表画板禁止装饰性眉题和描述性副标题、R1 必须提供可独立审核的 PC / mobile 业务画板等协作规则继续成立。
- `R1-P01` 两轮业务页面评审均未通过。第二轮虽然分开了分类、`#标签` 和结构化状态，也增加了分类、社区动态与相关内容，但帖子仍占据绝大多数视觉面积，全宽分类浏览笨重，页面没有形成参考 `13 / 16 / 18 / 27` 的异构工作台节奏。
- 两轮 `R1-P01` 画板只由 Git 留档，不是实现基线；灰玉颜色目标保留，但 `/discover`、运行时 token 和静态测试均不得在结构审核前提前修改。
- 今天没有修改 `/discover` 运行时代码、接口、数据库或 migration，也没有启动服务或执行真实 smoke。

## 今日全部提交

| 提交 | 主题 | 日终结论 |
| --- | --- | --- |
| `222e494b` | `feat(ui): 升级 family-ui v26.7.3 基线` | 同步上游 token 副本和 `text-on-brand`，并在 Client 与共享 Ant Design 层固定 Radish 四主题、品牌 / 操作前景边界；代码与契约测试结论有效。 |
| `f9016323` | `feat(ui): 建立单一版本化 Pencil 设计源` | 建立 `radish-web-family-ui-v1.pen`、索引、共享组件和 `R1-F01`；单一活动文件与旧稿只读留档规则有效。 |
| `0dfb61d2` | `feat(ui): 完成 R1-P01 公开内容流设计` | 形成首轮 PC / mobile / 状态稿并完成灰玉颜色校准；“R1-P01 已完成”的提交标题已被后续评审事实否定，不能作为当前状态。 |
| `baa78369` | `feat(ui): 收紧代表画板标题约束` | 清理装饰性眉题和描述性副标题，并把约束写入长期规范；该规则继续有效。 |
| `4e97b9d7` | `feat(ui): 形成 R1-P01 社区发现评审稿` | 分开分类、标签和结构化状态并尝试多区块首页；用户评审未通过，稿件只留档，不进入实现。 |

## 按代码反查文档

### 运行时与 token

- `Frontend/radish.ui/src/theme/family-ui-tokens.css` 与 JSON 副本已经升级到 `v26.7.3`，通用参考品牌为灰玉 `#5d6c57`。
- `Frontend/radish.client/src/theme/theme-tokens.css` 当前仍显式把 `guofeng` 固定为胭脂 `#b24057`；`theme.test.ts` 同时检查上游副本灰玉与 Client 运行时胭脂，证明“灰玉是待实现产品目标”而不是已上线事实。
- `Frontend/radish.ui/src/theme/antd-theme.ts` 继续使用胭脂品牌与墨蓝 Workbench 主操作，并通过 `familyThemeContract.test.ts` 固定两者不相等。
- 因此文档只保留灰玉设计目标，明确运行时实现必须等 `R1-P01` 结构通过后单独提交。

### `/discover` 当前事实

- `PublicDiscoverApp.tsx` 今天没有改动，仍使用 `getPostList(..., 'newest')`、`getHotTags`、单个公开文档、单个商品和榜单入口；没有接入第二轮画板提出的顶级分类、固定栏目或额外问答 / 投票区块。
- `publicSeoStatic.test.ts` 仍存在“公开社区发现页应保持单一社区内容主流并只加载少量辅助内容”的断言。这是旧页面治理的显式约束，与用户要求的多元社区方向发生冲突；下一轮设计通过后，实现批必须同步重新裁决该测试，不能让旧断言反向绑架页面结构。
- 当前代码仍渲染 `pageIntro` 与 `sectionDescription`；代表画板的“禁止描述性副标题”尚未实现，必须与最终获批页面一起收口，不能把 Pencil 规则误写成代码完成。
- 当前没有面向跨实体首页的统一公开读模型。明日应先把候选实体分为“现成可组合”“只有入口没有摘要数据”“需要新公开读模型”三类，再决定页面内容，而不是只从已有 `PostCard` 反推结构。

### 本次文档修正

- [当前进行中](/planning/current)、[开发路线图](/development-plan)和仓库 README 不再把 `R1-P01` 写成已完成或下一步直接实现，明日顺位改为参考重校准、能力审计和低保真布局比较。
- [公开发现专题](/features/discover-public-app)恢复当前代码事实，明确第二轮分区结构被否决，保留分类 / 标签 / 状态语义但取消全宽分类浏览和帖子主流的既定层级。
- [F4-R 专题](/features/family-ui-convergence-design)把 Public 社区主要参考从 `22 / 23` 调整为 `13 / 16 / 18 / 27`，前者只保留帖子卡片内部表达职责。
- [C-1A 审计](/frontend/f4-r-representative-page-audit)、[设计源目录](/frontend/design-sources/)、[UI 差异附录](/frontend/ui-addendum)、[视觉主题规范](/frontend/visual-theme-spec)和 [Pencil 协作流程](/frontend/pencil-representative-page-workflow)统一标记两轮评审未通过，并增加同文件低保真 `Layout Studies` 规则。
- 八月开发日志、年度日志和记录索引同步加入本次评审结论与日终入口；旧提交和 Git 历史不改写。

## 明日事项（2026-08-04）

1. 新会话先读取 [当前进行中](/planning/current)、本记录、F4-R 专题、公开发现专题和 C-1A 审计，不默认加载历史验收流水。
2. 重新查看 family-ui 参考 `13 / 16 / 18 / 27`，写出结构转译表：重点比较非对称主次、异构模块尺度、连续工作面、紧凑工具区和人员 / 任务 / 状态并置，不复制后台导航、品牌和业务字段。
3. 只读审计帖子、问答 / 投票、作者、圈子、Docs、商品、榜单以及其他可公开读取实体的 API、路由、权限和失败边界；按“可直接组合 / 只有入口 / 需要读模型”分类。
4. 在同一 `radish-web-family-ui-v1.pen` 中建立轻量 `R1-P01 / Layout Studies`，只画 `2~3` 个低保真 PC 构图比较：不用全宽分类浏览大块，首个主要视区至少并置三类经代码审计成立的实体，帖子不能占据绝大多数面积。
5. 用户选择结构方向后，再替换唯一 PC / mobile 代表画板和关键状态；不保留多套完整页面，不创建新 `.pen`。
6. 代表画板通过后再规划代码批：同步调整 `/discover`、旧“单一内容主流”静态测试、描述性副标题 locale / DOM 和灰玉运行时 token，并按风险执行定向测试、构建与 PC / mobile 复核。
7. 不提前进入 `R1-P02`，不默认启动服务，不恢复 Tauri，不扩 WebOS，不机械追平 Flutter，也不重启主动生产证据采集。

## 验证与留痕边界

- 日终重新执行 `@radish/ui` 测试：`30 / 30` 通过。
- 日终重新执行 `radish.client` 测试：`488 / 488` 通过。
- Pencil 文件已保存，节点布局、PC / mobile 与关键状态截图技术检查通过；该结论只表示文件完整，不等于业务或视觉审核通过。
- 本次日终文档批只执行文档文本卫生、仓库卫生和 Git 差异检查，不重复构建，不启动服务，不执行真实 smoke。

## 明日启动口径

明日第一目标不是继续美化当前稿，而是先证明新的社区首页构图真正多元、符合参考气质且建立在真实公开能力上。低保真结构未获选择前，不修改完整 PC / mobile 画板，不进入代码。
