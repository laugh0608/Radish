# v26.8.1 Release Candidate 变更回归记录（2026-08-15）

> 本记录覆盖 `origin/master@e03416ac` 之后准备通过 `dev -> master` 合并的完整阶段批次；记录对象是候选批次，不是单个提交。

## 变更摘要

- R3-F02 完成 OIDC 回流信任、自服务资料 / 设置权威状态、未知路由与错误恢复边界。
- Web / Console 完成四主题语义基线、宿主 reduced-motion 门禁和 Gateway 成组运行态验收，当前 Web 主题专题关闭。
- 多端路线收束为 Web / Flutter Native 两条产品线，建立 Flutter Native 产品化与 UI 重构专题；Tauri 弃用，WebOS 不扩展。
- CI 同规格 PostgreSQL 17 候选暴露并修复聊天已读游标并发创建后的单调收敛竞争，增强多目标并发回归。
- 产品版本从 `26.7.1` 提升到 `26.8.1`，准备 `v26.8.1-release` 正式发布记录。

## 影响专题

| 专题 | 影响判断 |
| --- | --- |
| Web / Console | 命中正式页面、自服务、错误边界、主题与 reduced-motion |
| 后端 / API | 命中 User Profile API、稳定自服务错误码、聊天已读游标仓储与测试 |
| 身份语义 | 命中 Auth OIDC Consent、state + PKCE 与协议消费者 |
| Flutter | 只命中统一版本和后续产品路线文档，无 Dart 运行时代码改动 |
| 部署 | 只准备版本与发布契约，未修改宿主、数据库、迁移或编排 |

## 候选提交范围

- `bde2e24f feat(self-service): 收口设置与资料权威状态`
- `42b0874d feat(boundary): 收口 R3-F02 错误与路由边界`
- `5aa22f9b fix(web): 收口 R3-F02 运行态验收`
- `7e9241fc docs(ui): 完成 Web 主题退出审计`
- `4cb1b10d feat(ui): 收口 Web 主题语义基线`
- `74f76149 feat(ui): 收口 reduced-motion 静态门禁`
- `7524d824 docs(ui): 完成 Web 四主题运行态验收`
- `b6a4bea0 docs(flutter): 确立 Native 产品与 UI 重构路线`
- 本次 `26.8.1` 版本同步、候选记录、规划门禁与 PostgreSQL 候选并发修复提交。

## 自动化执行

- `npm run version:sync`：通过，.NET、npm、Rust、Flutter 与冻结的 Tauri 历史资产均对齐 `26.8.1`。
- `node Scripts/version-contract.mjs --tag v26.8.1-release`：通过，产品版本、tag 和唯一正式发布记录元数据一致。
- `npm run validate:baseline`：通过；HTTP `48 / 48`、UI `32 / 32`、Client `557 / 557`、Console `138 / 138`，未注入 PostgreSQL 时后端 `1279 passed / 41 skipped / 0 failed`，构建 `0 warning / 0 error`。
- `npm run validate:ci -- --report-file .tmp/v26.8.1-postgres-fix-validate-ci-report.md`：通过；Backend Guard 与 Identity Guard 均命中并完成实际验证，结论为 `ready-for-pr-batch-record`。
- `npm run validate:backend`：由 `validate:ci` 执行并通过，后端 `1279 passed / 41 skipped / 0 failed`。
- `npm run validate:identity`：由 `validate:ci` 执行并通过，身份定向测试 `35 / 35`，Claim、协议输出和 LongId 扫描无回归。
- `RADISH_TEST_POSTGRES_CONNECTION_STRING=... npm run validate:candidate`：在 CI 同规格 PostgreSQL 17 下通过；全量前端 Lint 零 warning，warnings-as-errors 构建 `0 warning / 0 error`，后端 `1320 passed / 0 skipped / 0 failed`，全量仓库卫生预算无新增。
- `npm run check:dependency-security`：独立执行及 Candidate Quality 内复核均通过，npm / NuGet High / Critical 为 `0`。
- `npm run check:repo-hygiene:changed`、`git diff --check`：通过；索引篇幅提醒已在最终收口中消除。

## 专题与人工回归

- [R3-F02 成组运行态验收](/records/f4-r-r3-f02-grouped-runtime-acceptance-2026-08-15)：已完成匿名、Admin、非 Console 用户、PC / mobile、中英文、OIDC、自服务、未知路由与错误恢复矩阵。
- [Web 四主题成组运行态验收](/records/f4-r-t03-web-four-theme-grouped-runtime-acceptance-2026-08-15)：已完成四主题、中英文、PC / mobile、Public / Private / Author / Console 与 WebOS 历史兼容矩阵。
- 本次候选新增的仓储修复只影响聊天已读游标的数据一致性，不改变上述 Web 页面运行态行为，因此不重复启动服务或浏览器，不重启主动生产证据采集。

## PostgreSQL 候选阻断与修复

- 修复前的首次同规格全量候选为 `1319 passed / 1 failed / 0 skipped`；失败用例是聊天已读游标 PostgreSQL 并发推进，期望最终游标 `200`、实际为 `100`。
- 根因是并发创建尚未提交时条件更新命中 `0` 行，而随后的查询已能看到较小游标的新记录；旧逻辑直接返回该值，没有再次履行单调推进契约。
- 仓储内部增加一次有界的条件更新与权威状态复读；若数据库状态仍低于目标则显式失败，不引入进程锁、无限重试、接口或依赖变化。
- PostgreSQL 定向类测试 `4 / 4` 通过；增强后的 `8` 目标并发用例连续执行 `10` 轮通过，最终全量候选 `1320 passed / 0 skipped / 0 failed`。

## 时间语义门禁复核

- `Radish.Repository/SystemConfigStorageCoordinator.cs` 当前不存在 `DateTime.Now`；规划中“三处既有阻断”是历史修复完成后的陈旧描述，本批已按代码事实修正。
- 全仓既有时间语义由 baseline 预算继续约束，本批不扩大或重置 baseline。

## 部署复核

- 执行情况：未执行。
- 原因：当前只准备 `dev -> master` Release Candidate；推送、PR、合并、tag、镜像和生产部署均是后续独立动作。
- 回滚锚点：`v26.7.1.1204-release`。

## 当前结论

- 本地 Release Candidate 门禁已在 CI 同规格 PostgreSQL 17 下零跳过闭合，当前批次达到 PR-ready，可进入推送与 `dev -> master` PR；远程 required checks 通过前不得合并或创建正式 tag。

## 故障归类 / 环境边界

- Candidate Quality 首次在沙盒内执行时，前端静态服务器合约因临时监听 `127.0.0.1` 被沙盒拒绝而返回 `EPERM`；同一命令在沙盒外重跑后完整通过，归类为受限环境边界，不是代码或断言失败。
- 本地使用无持久卷的临时 `postgres:17` 容器复现 CI 数据库配置并完成零跳过 Candidate Quality；验证后容器已停止并自动删除。该结果消除了本地 PostgreSQL 覆盖缺口，但不替代远程 required checks。

## 风险 / 后置项

- 等待远程 `dev -> master` PR required checks，包括 PostgreSQL 17 Candidate Quality 的独立复核。
- Flutter Native 新 UI 不属于本次发布内容，下一批仍从 P1 事实审计开始。
