# P3-12-D40 UI 专题退出判断与发布候选前置清单

> 日期：2026-07-01（Asia/Shanghai）
>
> 类型：阶段退出判断 / 发布候选前置清单
>
> 状态：已确认 `P3-12-D` UI 专题具备退出条件；下一步进入 `P3-12-E1` 发布候选准备，不在本批创建 tag、PR 或发布记录

## 背景

`P3-12-D36` 明确 UI 专题不能在 Console 表格交互代码侧收口后直接结束，必须先完成设计源差距矩阵、边界裁决、真实页面阶段验收和文档口径同步。`P3-12-D37` 至 `P3-12-D39` 已分别补齐四类设计源矩阵、发布前边界裁决和 Gateway PC / mobile 阶段验收。

本批只做阶段退出判断和发布候选前置清单整理，不修改 Pencil、业务代码、API、权限、路由、保存动作或载荷。

## 退出条件对照

| D36 退出条件 | 已有证据 | D40 判断 |
| --- | --- | --- |
| public / private / foundation / console 四类设计源均有页面族对齐矩阵 | `P3-12-D37` 已按每个画板列出真实路由 / 页面族、代码状态、验证证据和剩余动作 | 满足 |
| 首轮落地页面区分文档确认、代码治理、Pencil 回拉和运行态观察 | `P3-12-D37` 矩阵与 `P3-12-D38` 裁决已把公开首页、公开聊天室、Console 内部平台和移动 Console 拆清楚 | 满足 |
| Console 表格、弹窗、抽屉、分页、工具条和移动窄屏完成成组判断 | `P3-12-D21-D35` 已完成静态收口、运行态复核、数据补验和表格交互代码侧治理；`P3-12-D39` 补订单详情、用户详情、角色权限和系统设置历史只读交互 | 满足 |
| 公开 Web 与私域 / 作者态页面完成首轮落地状态复核 | `P3-12-D39` 覆盖 public 17 个、private / author 22 个代表页面的 PC 与移动 CSS 视口 | 满足 |
| 运行态确认项目进入阶段性 Gateway PC / mobile 验收 | `P3-12-D39` 已通过 `check:host-runtime -- --details`，并完成 53 个代表页面 PC / mobile CSS 视口扫描 | 满足 |
| `current`、P3-12 专题、记录和 changelog 阶段口径一致 | 本批同步规划入口、P3-12 专题页、记录索引和 2026-07 日志 | 满足 |

## 退出结论

`P3-12-D` UI 专题已具备阶段退出条件。D36-D40 已完成从差距识别、范围裁决、真实页面验收到退出判断的完整链路；当前没有发现需要阻断进入发布候选准备的 UI 专题问题。

`P3-12-E` 可以启动，但第一步应是 `P3-12-E1` 发布候选准备：整理验证矩阵、PR 范围、剩余风险、回滚口径和正式验收入口。该结论不等同于已完成发布候选验收，也不等同于可以直接创建 tag 或进入部署。

## 发布前仍保留的边界

- 公开首页 `P01` 发布前由 `/discover` 承接，不新增独立 `PublicHomeApp`。
- 公开聊天室 `P15 / P16` 后置评审；若要推进，先补公开聊天室产品方案、实时协议、登录 / 匿名边界和治理策略。
- Console 内部调度中心、内部 Jobs 平台和独立移动 Console 应用后置，不作为当前发布候选前置项。
- `/workbench` 继续承接低频入口和功能地图，不回退 WebOS 默认路径。
- WebOS `/desktop` 保留为历史兼容入口，新功能默认不进入 WebOS。

## 剩余风险与观察项

- D39 移动端覆盖的是 `390x844` CSS 视口，未形成 DPR 3 物理高分屏完整结论。
- D39 未执行保存、删除、重试、上传、购买、发布等会改变数据的动作；发布候选准备阶段如需覆盖，应使用安全测试数据和可回滚步骤。
- `/docs/mine` 未暴露普通 `/docs/edit/:id` 链接，Docs 编辑详情未写成已验证。
- 系统设置页面曾观察到 0 尺寸隐藏编辑弹窗节点，当前未造成可见遮挡或布局问题；若后续出现焦点、快捷键或浮层问题，再回拉治理。
- Playwright CLI wrapper 本轮初始化未稳定返回，D39 改用 Codex in-app Browser 完成真实页面复核；这不是产品阻断，但发布候选阶段可优先使用稳定工具链复核。

## `P3-12-E1` 前置清单

1. 固定发布候选范围：刷新 `master..dev` 与本地工作区，确认 P3-12 范围、未合并提交和不进入发布的后置项。
2. 整理验证矩阵：覆盖 `validate:baseline`、`validate:identity`、`validate:baseline:host`、必要的 `validate:ci -- --report` 和 Gateway PC / mobile 页面复核。
3. 按 D39 页面矩阵补发布候选级页面记录；若要覆盖保存 / 上传 / 购买 / 发布等写动作，先限定安全测试数据、可回滚方式和不影响正式数据的边界。
4. 复核数据库与配置发布风险：当前本地 SQLite 阶段不维护历史发布 SQL；若出现正式数据库，补结构 SQL 审核、回滚说明和配置差异确认。
5. 准备 `dev -> master` PR 材料：提交范围、验证结论、剩余风险、回滚策略和不进入本次发布的后置功能。
6. 在 PR / 发布明确获批前，不创建 tag、不进入 M15 测试 / 生产部署流程。

## 下一步

进入 `P3-12-E1`：发布候选准备与验证矩阵整理。若验证暴露阻断问题，再按问题归属回到对应功能迁移、WebOS 残留或 UI 专题页面族定向修复。
