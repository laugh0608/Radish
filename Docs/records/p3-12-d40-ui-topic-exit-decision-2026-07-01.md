# P3-12-D40 UI 专题退出判断修正与页面开发回拉

> 日期：2026-07-01（Asia/Shanghai）
>
> 类型：阶段退出判断修正 / 页面开发缺口回拉
>
> 状态：已确认 `P3-12-D` UI 专题不能退出；下一步继续留在 `P3-12-D41` 做页面开发缺口排序与成组实现，不进入发布候选准备

## 背景

`P3-12-D36` 明确 UI 专题不能在 Console 表格交互代码侧收口后直接结束，必须先完成设计源差距矩阵、边界裁决、真实页面阶段验收和文档口径同步。`P3-12-D37` 至 `P3-12-D39` 已分别补齐四类设计源矩阵、发布前边界裁决和 Gateway PC / mobile 阶段验收。

上一版 D40 把 D39 的“已有代表页面运行态验收通过”误提升为“UI 专题可以退出并进入发布候选准备”。该判断不成立：代表页面能挂载和无阻断，并不等于 Pencil 设计源里的页面已经全部完成开发、接入真实数据、覆盖关键操作和完成 PC / mobile 复核。

本批修正 D40 结论，不修改 Pencil、业务代码、API、权限、路由、保存动作或载荷。

## 退出条件对照

| D36 退出条件 | 已有证据 | D40 判断 |
| --- | --- | --- |
| public / private / foundation / console 四类设计源均有页面族对齐矩阵 | `P3-12-D37` 已按每个画板列出真实路由 / 页面族、代码状态、验证证据和剩余动作 | 满足 |
| 首轮落地页面区分文档确认、代码治理、Pencil 回拉和运行态观察 | `P3-12-D37` 矩阵与 `P3-12-D38` 裁决已把公开首页、公开聊天室、Console 内部平台和移动 Console 拆清楚 | 只完成范围裁决，尚未完成“设计源页面是否真实开发完”的源码级复核 |
| Console 表格、弹窗、抽屉、分页、工具条和移动窄屏完成成组判断 | `P3-12-D21-D35` 已完成静态收口、运行态复核、数据补验和表格交互代码侧治理；`P3-12-D39` 补订单详情、用户详情、角色权限和系统设置历史只读交互 | 只证明 Console 已迁移页面首轮治理有效，不证明 `P00-P18` 设计源全量完成 |
| 公开 Web 与私域 / 作者态页面完成首轮落地状态复核 | `P3-12-D39` 覆盖 public 17 个、private / author 22 个代表页面的 PC 与移动 CSS 视口 | 只证明代表页面可访问，不证明全部设计页面已开发、数据态已覆盖或关键动作已验证 |
| 运行态确认项目进入阶段性 Gateway PC / mobile 验收 | `P3-12-D39` 已通过 `check:host-runtime -- --details`，并完成 53 个代表页面 PC / mobile CSS 视口扫描 | 验收有效，但范围不足以支撑发布候选准备 |
| `current`、P3-12 专题、记录和 changelog 阶段口径一致 | 本批修正规划入口、P3-12 专题页和 2026-07 日志 | 修正后满足 |

## 退出结论

`P3-12-D` UI 专题不具备退出条件，不能进入 `P3-12-E` 发布候选准备。

D39 的正确价值是：确认当前已有代表页面在 Gateway 下没有阻断级挂载、登录误跳、授权停留或横向溢出问题。它不能替代后续页面开发，也不能替代设计源逐页落地检查。

## 继续保留的范围裁决

- 公开首页 `P01` 发布前由 `/discover` 承接，不新增独立 `PublicHomeApp`。
- 公开聊天室 `P15 / P16` 后置评审；若要推进，先补公开聊天室产品方案、实时协议、登录 / 匿名边界和治理策略。
- Console 内部调度中心、内部 Jobs 平台和独立移动 Console 应用后置。
- `/workbench` 继续承接低频入口和功能地图，不回退 WebOS 默认路径。
- WebOS `/desktop` 保留为历史兼容入口，新功能默认不进入 WebOS。

## D41 前置问题

- D37 的“已落地”状态主要来自既有记录和路由存在性，D41 需要回到源码逐页确认真实开发完成度。
- D37 的“首轮落地待观察”不能视为完成，需要区分只是布局待复核，还是页面数据态、交互动作、移动任务流仍缺开发。
- D39 未执行保存、删除、重试、上传、购买、发布等会改变数据的动作，不能把作者态、交易态或 Console 写动作写成已验收。
- `/docs/mine` 未暴露普通 `/docs/edit/:id` 链接，Docs 编辑详情未写成已验证。
- D39 移动端只覆盖 `390x844` CSS 视口，未形成 DPR 3 物理高分屏完整结论。

## `P3-12-D41` 工作清单

1. 以 D37 矩阵为输入，重新按源码和真实路由逐页标注：已开发可复核、已设计但未开发、只首轮外壳、已裁决后置、需补设计。
2. 对 `public / private / author / console` 分别列出页面开发缺口，不再用“代表页面可访问”替代“页面族开发完成”。
3. 优先推进真实用户主路径中设计已有、产品边界明确、代码未完成或只完成外壳的页面族。
4. 继续保持 D38 后置裁决：公开聊天室、内部调度中心、内部 Jobs 平台和独立移动 Console 应用不混入当前页面开发批次。
5. 每完成一组页面开发后，再做对应的精准构建 / 类型检查 / PC / mobile 复核；发布候选准备后置到 UI 专题真实退出之后。

## 下一步

进入 `P3-12-D41`：页面开发缺口排序与成组实现准备。先输出开发缺口清单，再按页面族推进代码实现和定向验证。
