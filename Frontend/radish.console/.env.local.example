# 本地开发环境配置（不提交到 Git）
# 复制此文件为 .env.local 并修改配置

# API 基础 URL（通过 Gateway 访问）
VITE_API_BASE_URL=https://localhost:5000

# Auth Server URL（通过 Gateway 访问）
VITE_AUTH_SERVER_URL=https://localhost:5000

# 是否启用调试模式
VITE_DEBUG=true
